# 24 回忆卷解析与考点加固

## 使用定位

本文件根据回忆卷整理。题干完整、答案可唯一判断的题给出答案；题干或选项缺失的题标为“无法唯一确定”，但仍给出对应知识点和解题方法。

下面按“单独题面 + 折叠答案”的形式整理：先独立看题面作答，再展开答案核对。

复习时不要把本文件当作押题，而要用它检查自己是否会：

- 用英文术语判断概念题。
- 对自动机、LR、语义环境、IR、trace、liveness、寄存器分配做手算。
- 写出中间步骤，而不是只写最终答案。

## 按章节使用回忆卷

这份回忆卷不是新的复习目录。复习顺序仍按 `01 → 18` 主线走；本文件只用于告诉你每章大概会怎么考、哪些拓展不用深挖。

| 章节 | 高频考点 | 最低掌握标准 |
|---|---|---|
| `01-02` | IR 降低转换数量、RE 等价、NFA/DFA/最小化 | 会按 PPT 算，不做正则语言证明 |
| `03-06` | CFG、FOLLOW、LR(1) item、Yacc shift/reduce | 会 closure/lookahead/表格，不背全部 parser generator 语法 |
| `08` | Tiger TEnv/VEnv、作用域、shadowing、type-check | 会逐作用域列新增绑定 |
| `09-12` | 栈帧、左值、IR、canonical tree、tiling | 会判断概念和画常见 Tree IR |
| `13-14` | liveness、干涉图、freeze、spill、coalescing | 会写步骤，图可按题目给定信息推进 |
| `16` | RC/copying GC、pointer map/type descriptor | 会判断 true/false 和做小规模手算；generational/incremental/hybrid 不考算法细节 |
| `17` | OO、单继承布局、多继承概念 | 课件标注不考，只作回忆卷概念兜底，不作为主攻 |
| `18` | dominator、natural loop、loop invariant | Part I 主线要会；Part II 的 induction/bounds/unrolling 标注不考，速读即可 |

答题优先级：

1. 大题先写模板和中间步骤，哪怕最终图有小错也能拿过程分。
2. 判断题必须写出“为什么错”，尤其是 `some/all`、`always`、`directly`、`all unreachable` 这类绝对词。
3. 单选题先把英文术语翻译成章节概念，再排除与定义冲突的选项。

## 判断题

### 判断题 1

**题面：** `(a|c)*|(a|c)*b(a|c)*` 与 `(a|c)*(b|ε)(a|c)*` equivalent

<details class="answer-drawer">
<summary>答案与解析</summary>

**答案：True**

断句先按正则优先级来：

```text
(a|c)* | (a|c)* b (a|c)*
(a|c)* (b|ε) (a|c)*
```

第一式左边 `(a|c)*` 表示没有 `b` 的串；右边 `(a|c)* b (a|c)*` 表示恰好出现一个 `b`，前后只能是 `a/c`。

第二式中间 `(b|ε)` 表示“选一个 `b` 或什么都不选”。因此它也表示没有 `b` 或恰好一个 `b`。

所以两式都表示：

```text
over alphabet {a,b,c}, 且 b 的出现次数 ≤ 1
```

例如 `acac`、`acbca`、`b` 可以匹配；`abb`、`bacb` 不能匹配。

</details>

### 判断题 2

**题面：** There exists a grammar that is LL(1) but not LR(1)

<details class="answer-drawer">
<summary>答案与解析</summary>

**答案：False**

核心知识点是包含关系：

```text
LL(1) grammars ⊂ LR(1) grammars
```

LL(1) parser 每一步是：

```text
看栈顶非终结符 A + 当前 1 个 lookahead token
唯一决定用 A → α 中哪一条产生式
```

LR(1) parser 更强，它从左到右读入输入，允许先 shift 一些符号，再根据 item 和 1 个 lookahead 决定 reduce。能被 LL(1) 预测分析处理的确定性选择，LR(1) 也能处理。

所以“不存在一个文法是 LL(1) 但不是 LR(1)”；题目说 there exists，因此为 False。

</details>

### 判断题 3

**题面：** Instruction Select is to find an appropriate machine instruction for each intermediate tree node

<details class="answer-drawer">
<summary>答案与解析</summary>

**答案：False**

Instruction selection 的输入通常是一棵 IR tree，目标是用机器指令模板覆盖它：

```text
IR tree node/subtree  →  machine instruction tile
```

错点在 `for each intermediate tree node`。指令选择不是严格“一棵 IR tree 的每个节点对应一条机器指令”。一个 tile 可以覆盖多个 IR 节点，例如：

```text
MEM(BINOP(PLUS, TEMP fp, CONST k))
```

可能直接对应一条带偏移寻址的 load/store 指令，而不是 `PLUS` 一条、`MEM` 又一条。

因此正确说法应是：

```text
find a set of instruction patterns/tiles that cover the IR tree
```

</details>

### 判断题 4

**题面：** Maximum munch always produce optimal tiling

<details class="answer-drawer">
<summary>答案与解析</summary>

**答案：True**

这题要按课件术语区分 `optimal` 和 `optimum`。

Maximal Munch 的步骤是：

```text
1. 从 root 开始。
2. 找能匹配当前 root 的最大 tile。
3. 输出该 tile 对应的机器指令。
4. 对 tile 没覆盖到的子树递归处理。
```

虎书/PPT 里常说 Maximal Munch 可以产生 `optimal tiling`，意思是每一步都选一个合法的大 tile，局部覆盖不会留下无法覆盖的树。

但它不保证 `optimum tiling`：

```text
optimum = all possible tilings 中 cost 最低的全局最优
```

全局最低 cost 通常要用 dynamic programming 才能保证。若题目写的是 `lowest-cost`、`minimum-cost` 或 `optimum tiling`，才应判 False。

</details>

### 判断题 5

**题面：** If precedence and associativity declarations are not specified, YACC shifts by default to resolve a shift/reduce conflict

<details class="answer-drawer">
<summary>答案与解析</summary>

**答案：True**

Yacc/Bison 在某个状态看到下一个 token 时，如果同时有两种动作：

```text
shift:  读入这个 token，进入下一个状态
reduce: 用某条产生式把栈顶符号归约
```

这就是 shift/reduce conflict。

如果没有 `%left`、`%right`、`%nonassoc` 这类 precedence/associativity 声明帮助它选择，Yacc 的默认策略是：

```text
shift/reduce conflict → shift
```

典型例子是 dangling else：看到 `else` 时默认 shift，让 `else` 绑定最近的 `if`。

补充区分：

```text
reduce/reduce conflict → 默认选文法中先出现的产生式
```

但实际写 parser 时不应依赖默认冲突，应通过改文法或加优先级/结合性声明把冲突解释清楚。

</details>

### 判断题 6

**题面：** Given 5 languages and 4 target machines with an IR, we should implement 20 transitions

<details class="answer-drawer">
<summary>答案与解析</summary>

**答案：False**

这是编译器分层的经典计算题。

如果没有共享 IR，每一种源语言都要直接连到每一种目标机器：

```text
5 languages × 4 target machines = 20 translators
```

如果引入统一 IR，结构变成：

```text
source language → IR → target machine
```

需要的是：

```text
5 个 front ends: language → IR
4 个 back ends: IR → machine
合计 5 + 4 = 9
```

题目说“with an IR, we should implement 20 transitions”，把有 IR 和无 IR 的数量混了，所以为 False。

</details>

### 判断题 7

**题面：** Global graph coloring directly in Multiple Inheritance leave empty slots in objects

<details class="answer-drawer">
<summary>答案与解析</summary>

**答案：True**

题目里的关键词是 `directly in objects`。

多继承里，不同父类的字段布局要尽量保持访问兼容。全局图着色会给字段分配 color：

```text
field f1 → color 0
field f2 → color 3
field f3 → color 5
```

如果直接把 color 当成 object slot，那么某个类只用了 color 0 和 color 3 时，对象布局会变成：

```text
slot 0: used
slot 1: empty
slot 2: empty
slot 3: used
```

这些没用到但必须保留位置的 slot 就是 empty slots。

如果改用 descriptor coloring，可以把间接查找信息放到 descriptor 中，使对象本体更紧凑。题目说的是 direct coloring on objects，所以判 True。

</details>

### 判断题 8

**题面：** Reference counting can reclaim all the unreachable objects

<details class="answer-drawer">
<summary>答案与解析</summary>

**答案：False**

Reference counting 的回收条件是：

```text
object.reference_count == 0
```

但 unreachable 的对象不一定 reference count 为 0。循环引用就是反例：

```text
roots 不再指向 A/B

A → B
B → A
```

从 roots 出发，`A` 和 `B` 已经不可达，应当算垃圾。但计数仍是：

```text
refcount(A) = 1   // 来自 B
refcount(B) = 1   // 来自 A
```

二者都不是 0，因此单纯 reference counting 不会回收它们。

所以正确说法应是：

```text
Reference counting can reclaim objects whose count drops to zero,
but cannot reclaim all unreachable objects by itself.
```

</details>

### 判断题 9

**题面：** If function has call-by-reference parameter, it can return address of this parameter without dangling-reference problem

<details class="answer-drawer">
<summary>答案与解析</summary>

**答案：通常 True，但依赖实际对象生命周期**

按引用传参时，形参不是 callee 新建的普通局部副本，而是引用 caller 传来的对象：

```text
caller:
  var y
  f(y)

callee:
  f(ref x)  // x 引用 caller 的 y
```

如果 `f` 返回 `x` 所引用对象的地址，地址指向的是 caller 的 `y`，不是 `f` 栈帧里某个局部变量。因此 `f` 返回、callee 栈帧销毁时，这个地址不会因为 callee 结束而立刻 dangling。

对比真正危险的情况：

```text
f():
  var local
  return &local   // local 在 f 返回后消失，dangling
```

不过这句话不能理解成“永远不会 dangling”。如果 caller 那个对象后来也结束生命周期，返回的地址仍会悬空。

考试里若考点是“call-by-reference parameter 引用 caller object，而不是 callee local copy”，通常判 True；若题目强调 absolute/always no dangling，则要指出生命周期条件。

</details>

### 判断题 10

**题面：** Hashing table with external chaining is mentioned as nondestructive, efficient data structure in functional style symbol table

<details class="answer-drawer">
<summary>答案与解析</summary>

**答案：False**

`hash table with external chaining` 是这种结构：

```text
bucket[i] → entry1 → entry2 → ...
```

它通常很高效，但常见实现是 destructive update：

```text
insert x:
  newEntry.next = bucket[h(x)]
  bucket[h(x)] = newEntry
```

也就是直接修改原来的 bucket 链表。

`functional style symbol table` 强调 nondestructive/persistent：插入新绑定时不改旧环境，而是返回一个新环境，旧环境还可用于退出作用域后恢复。

所以本题错在把：

```text
external chaining hash table = efficient, usually imperative/destructive
```

说成：

```text
nondestructive functional-style data structure
```

它“高效”这半句可以对，但“nondestructive, functional style”不对，因此整体 False。

</details>

### 判断题 11

**题面：** A node `d` dominates a node `n` if some path from start node `s0` to `n` goes through `d`

<details class="answer-drawer">
<summary>答案与解析</summary>

**答案：False**

支配关系的定义是：

```text
d dominates n
⇔ every path from start node s0 to n goes through d
```

题目写的是 `some path`，只要求“存在一条路径经过 `d`”，条件太弱。

反例：

```text
s0 → d → n
s0 → x → n
```

这里确实有一条路径经过 `d`，但还有一条路径绕过 `d`，所以 `d` 不支配 `n`。

判断 dominator 时看到：

```text
some path / one path
```

基本要警惕；正确关键词是 `all paths` / `every path`。

</details>

## 单选题解析

### 1. 正则表达式 `a|b(ab)[ab]*`

**题面：** 1. 正则表达式 `a|b(ab)[ab]*`

<details class="answer-drawer">
<summary>答案与解析</summary>

由于连接优先级高于并，表达式等价于：

```text
a | (b a b [ab]*)
```

判断时按三步来：

```text
1. 先处理括号和闭包：[ab]* 表示任意多个 a 或 b。
2. 再处理连接：b(ab)[ab]* 必须以 b a b 开头。
3. 最后处理 |：整个语言是 {a} 和 b a b [ab]* 的并集。
```

能匹配：

```text
a
bab
baba
babb
babaa
...
```

不能匹配：

```text
ε
b
ab
ba
aa
```

缺少选项，无法给唯一选项。考试中先加括号再判断字符串是否属于语言。

如果选项里有字符串，可以用这个模板判：

```text
是否正好是 a？
  是 → 匹配
  否 → 是否以 bab 开头，后面只含 a/b？
       是 → 匹配
       否 → 不匹配
```

</details>

### 2. LR(1) item 同状态

**题面：** 文法和当前 item 如下，判断 closure 后哪个 LR(1) item 会出现在同一状态。

```text
A → ( A ) | a
```

```text
[A → ( . A ), $]
```

<details class="answer-drawer">
<summary>答案与解析</summary>

点后是非终结符 `A`，closure 要加入 `A` 的产生式。新 lookahead：

```text
FIRST(")" $) = { ")" }
```

LR(1) closure 规则是：

```text
若 [B → α . A β, a] 在项集中，
对每个 A → γ，
以及每个 b ∈ FIRST(βa)，
加入 [A → . γ, b]。
```

本题中：

```text
B = A
α = (
A = A
β = )
a = $
```

所以：

```text
FIRST(βa) = FIRST(")" $) = { ")" }
```

所以加入：

```text
[A → . ( A ), )]
[A → . a, )]
```

给出的选项中正确的是：

```text
[C] [A → .(A), )]
```

`[A]` 和 `[B]` 的 lookahead `$` 错；`[D]` 是移进并归约后的完整项，不在当前 closure 状态里。

易错点：closure 是“把点后非终结符的产生式加进来”，不是执行一次 shift。因此点仍在产生式最前面：

```text
[A → . ( A ), )]
[A → . a, )]
```

</details>

### 3. Follow Set

**题面：** 3. Follow Set

<details class="answer-drawer">
<summary>答案与解析</summary>

题干缺文法，无法计算。标准步骤：

```text
1. FOLLOW(start) 加 EOF。
2. 对 A → α B β，把 FIRST(β)-{ε} 加入 FOLLOW(B)。
3. 若 β 可空或为空，把 FOLLOW(A) 加入 FOLLOW(B)。
4. 迭代直到不变。
```

更具体地说，考试做 FOLLOW 时不要凭感觉看“后面可能跟什么”，而要逐条产生式扫：

```text
产生式: A → α B β

case 1: β 不是空，且 β 不能推出 ε
  FOLLOW(B) 加 FIRST(β)

case 2: β 能推出 ε
  FOLLOW(B) 加 FIRST(β)-{ε}
  FOLLOW(B) 还要加 FOLLOW(A)

case 3: B 已经在产生式最右侧
  FOLLOW(B) 加 FOLLOW(A)
```

小例子：

```text
S → A B
A → a | ε
B → b
```

计算：

```text
FOLLOW(S) = {$}
A 后面是 B，所以 FOLLOW(A) 加 FIRST(B) = {b}
B 在 S → A B 最右侧，所以 FOLLOW(B) 加 FOLLOW(S) = {$}
```

最后：

```text
FOLLOW(S) = {$}
FOLLOW(A) = {b}
FOLLOW(B) = {$}
```

回忆题缺具体文法时不能给唯一数值答案，但完整答案至少要写出这些规则和迭代过程。

</details>

### 10. Loop Variant / Loop Invariant

**题面：** 给定回忆题片段，判断哪个变量不随循环迭代变化 / 是 loop-invariant。

```text
i <- 0
j <- i
Loop:
  a <- j + 1
  b <- a * c
  c <- ...
  i <- i + 1
  if ... goto Loop
```

选项回忆为：

```text
[A] a  [B] b  [C] c  [D] d
```

<details class="answer-drawer">
<summary>答案与解析</summary>

若题目问“which variable is not loop-variant / is loop-invariant”，在已知片段中：

- `j` 在循环外定义，循环内不改。
- `a = j + 1` 每轮计算结果相同，因此 `a` 是 loop-invariant computation 的结果。
- `b = a * c` 取决于 `c`，若 `c` 在循环内改变，则不是 invariant。
- `c` 在循环内定义，通常 variant。

判断步骤：

```text
1. 先列循环内被重新赋值的变量：a, b, c, i。
2. 再看每个赋值右侧是否只依赖循环不变量。
3. j 在循环前定义且循环内不改，所以 j invariant。
4. a = j + 1，只依赖 j 和常量 1，所以 a 的计算结果 invariant。
5. b = a * c，依赖 c；c 在循环内被改，所以 b 通常 variant。
6. i 每轮 i <- i + 1，variant。
```

在给出的选项 `[A] a [B] b [C] c [D] d` 中，若问“不随迭代变化的变量”，倾向选 `[A] a`。但题干缺完整 `c <- ...` 和问题原文，不能保证唯一。

易错点：`a` 虽然在循环内被赋值，但“赋值语句在循环内”不等于“值随迭代变化”。如果右侧表达式每轮都一样，计算结果仍可视为 loop-invariant。

</details>

### 12. 关于 Freeze

**题面：** 关于寄存器分配中的 Freeze，判断哪项表述错误。

```text
A. freeze 是在不能 simplify 也不能 coalesce 后进行
B. freeze 优先查看 degree 较小的 move-related 节点
C. freeze 将两个原来 move related 的边变成 non move related
D. freeze 后可以继续 simplify 和 coalesce
```

<details class="answer-drawer">
<summary>答案与解析</summary>

逐项判断：

| 选项 | 判断 | 解释 |
|---|---|---|
| A. freeze 是在不能 simplify 也不能 coalesce 后进行 | True | 典型流程：Simplify/Coalesce 都不能推进时 Freeze。 |
| B. freeze 优先查看 degree 较小的 move-related 节点 | True | 常从低度数 move-related 节点入手，冻结相关 moves 后可进入 simplify。 |
| C. freeze 将两个原来 move related 的边变成 non move related | 表述不准，倾向 False | freeze 是冻结 move，把 move 从待合并集合移到 frozenMoves，不是改变 interference edge。 |
| D. freeze 后可以继续 simplify 和 coalesce | True | 冻结后节点可能不再 move-related，可进入 simplify；其他 move 仍可能 coalesce。 |

若考试问“错误的是”，优先选 C。

背流程时可以按 Appel 图着色寄存器分配的工作表理解：

```text
1. simplify: 找非 move-related 且 degree < K 的节点，删掉压栈。
2. coalesce: 尝试合并 move x <- y 的两个端点。
3. freeze: 如果 simplify/coalesce 都暂时推进不了，选低度数 move-related 节点，把相关 move 冻结。
4. spill: 如果还不行，选择可能 spill 的节点。
```

freeze 的效果是：

```text
move 从 active/worklist move 集合 → frozenMoves
节点可能从 move-related 变成 non-move-related
```

它不是把 interference edge 改成另一种边，也不是删除真实冲突边。真实 interference edge 仍然表示两个 temporary 不能共用寄存器。

</details>

### 15. Copying Collection 优点

**题面：** 15. Copying Collection 优点

<details class="answer-drawer">
<summary>答案与解析</summary>

不属于 copying collection 优点的是：

```text
[C] save memory
```

原因：copying collection 通常需要 from-space/to-space 两块空间，空间开销较大。它的优点包括：

- 能回收循环垃圾，因为基于 reachability。
- 消除外部碎片，使对象紧凑。
- 分配快，通常 bump pointer。

推理顺序：

```text
copying collection 会把 live objects 从 from-space 复制到 to-space。
复制后 live objects 连续排列。
下次分配只需要移动 free pointer。
```

因此它的优点是 compaction 和 fast allocation。

但代价是：

```text
通常要预留 from-space / to-space
同一时刻只能使用其中一部分作为分配空间
```

所以说它 `save memory` 不对。

</details>

### 哪种变量需要存在栈帧里

**题面：** 哪种变量需要存在栈帧里

<details class="answer-drawer">
<summary>答案与解析</summary>

确定正确项：

```text
[B] address taken 的
```

取地址变量必须有稳定内存地址，不能只放寄存器。

补充：

- 被内层函数引用的变量也通常 escape，需要放 frame。
- “其值被传给其他函数”不一定 escape；如果只是按值传递，不需要放 frame。若按引用传递或地址传出，则 escape。

判断变量是否必须放 frame，可以按这个顺序：

```text
1. 是否 address taken？
   是 → 必须有稳定内存地址，放 frame。

2. 是否被内层嵌套函数引用？
   是 → 外层函数返回前，内层函数要通过 static link/frame 找到它，通常放 frame。

3. 是否跨 call 仍然 live？
   不一定必须放 frame，也可以存在 callee-save register 或 spill slot，取决于寄存器分配。

4. 是否只是值传给其他函数？
   按值传递不代表 escape，不一定放 frame。
```

所以选项里如果有 `address taken`，通常就是最稳的答案。

</details>

### IR Tree 执行后 `a` 的值

**题面：** 已知 `TEMP fp = 10`，执行下面 IR Tree 后判断 `a` 的值。

```text
TEMP fp = 10
SEQ(
  CJUMP(LT, TEMP(fp), CONST(5), L1, L3),
  LABEL(L1),
  MOVE(TEMP(a), 1),
  JUMP(L3),
  LABEL(L2),
  MOVE(TEMP(a), 2),
  LABEL(L3)
)
```

给出的选项回忆为 `[1, 2, 5, 10]`。

<details class="answer-drawer">
<summary>答案与解析</summary>

判断：

```text
10 < 5 为 false
```

因此 `CJUMP` 跳到 `L3`，会跳过 `LABEL L1` 和 `MOVE(TEMP(a),1)`，也不会到 `L2`。所以 `a` 没有被赋新值。

给出的 `[1,2,5,10]` 都不是严格正确答案；若考题必须单选，原题可能漏了初始 `a` 值或跳转目标。按 IR 控制流语义，答案应是 `a` unchanged/undefined。

逐条执行可以写成：

```text
初始: fp = 10

1. 执行 CJUMP(LT, TEMP(fp), CONST(5), L1, L3)
2. 比较 10 < 5，结果 false
3. false label 是 L3，所以控制流直接跳到 LABEL L3
4. 中间的 LABEL L1、MOVE(TEMP(a),1)、JUMP(L3)、LABEL L2、MOVE(TEMP(a),2) 都不会执行
5. 到达 LABEL L3 后没有新的 MOVE(TEMP(a), ...)
```

因此除非题目额外给了 `a` 的初始值，否则 `a` 的值无法从这段 IR 推出。

</details>

### Liveness Analysis 在什么过程中使用

**题面：** Liveness Analysis 在什么过程中使用

<details class="answer-drawer">
<summary>答案与解析</summary>

正确：

```text
[A] register allocation
```

liveness 用于构造 interference graph，决定哪些 temporaries 不能共用寄存器。

典型数据流链条是：

```text
程序语句
→ use/def/succ
→ live-in/live-out
→ interference graph
→ graph coloring
→ register allocation
```

如果两个 temporary 在同一点同时 live，它们不能分配同一个寄存器，就要在 interference graph 里连边。

所以看到 `liveness analysis`，第一反应应是：

```text
register allocation
```

</details>

### Liveness 定义题

**题面：** 判断下列 liveness 定义相关选项是否正确。

```text
如果变量 a 在 node n 的 live-in 中，则所有 proc[n] 的 live-out 中有 a
```

<details class="answer-drawer">
<summary>答案与解析</summary>

若 `proc[n]` 指 predecessors，则 C 是 True：

```text
out[p] = ⋃_{s ∈ succ[p]} in[s]
```

只要 `n` 是 `p` 的 successor，`a ∈ in[n]` 就会使 `a ∈ out[p]`。

若原文其实是 successors 或 all paths，则不能这样判断。需要完整选项。

展开逻辑：

```text
a ∈ in[n]
```

表示变量 `a` 在进入节点 `n` 前仍可能被之后使用。

若 `p` 是 `n` 的 predecessor，则：

```text
n ∈ succ[p]
```

而 live-out 定义为：

```text
out[p] = union of in[s] for all s in succ[p]
```

既然 `n` 是 `p` 的一个 successor，`in[n]` 中的 `a` 会被并入 `out[p]`。

注意题目里写的 `proc[n]` 很可能是 `pred[n]` 的回忆误写；如果原题是 `succ[n]` 或 “所有后继路径都包含”，就要重新判断。

</details>

### 不符合 Left Value 的选项

**题面：** 不符合 Left Value 的选项

<details class="answer-drawer">
<summary>答案与解析</summary>

正确：

```text
[A] BINOP(ADD, CONST(1), CONST(2))
```

`BINOP` 计算一个值，不能作为赋值目标。`TEMP x` 和 `MEM(...)` 可以作为 `MOVE` 左侧，因此是 l-value。

Tree IR 里可以出现在 `MOVE(dst, src)` 左侧的典型形式是：

```text
TEMP t      // 给临时变量赋值
MEM(e)      // 给地址 e 指向的内存赋值
```

`BINOP(ADD, CONST(1), CONST(2))` 只是算出值 `3`，没有可写位置。它类似 C 语言里的：

```c
1 + 2 = x;   // 非法
```

所以它不是 left value。

</details>

### 构造规范树的错误说法

**题面：** 构造规范树的错误说法

<details class="answer-drawer">
<summary>答案与解析</summary>

错误：

```text
[D] Eliminate all CJUMPs
```

canonicalization 会消除 `ESEQ`、处理嵌套 `CALL`、线性化 `SEQ`，但不会消除所有 `CJUMP`。`CJUMP` 是控制流的基本语句。

canonical tree 的常见目标：

```text
1. 去掉 ESEQ。
2. 把 CALL 提到合适位置，避免 CALL 嵌套在表达式深处。
3. 把 SEQ 线性化成 statement list。
4. 再划分 basic blocks，并用 trace 优化控制流布局。
```

`CJUMP` 不会被“全部消除”，因为条件跳转是 basic block 结尾的合法控制流语句。后续 trace 排列可能会调整 true/false label 或补 jump，但不是把所有条件跳转删掉。

</details>

### Stack Frame 最精准表述

**题面：** Stack Frame 最精准表述

<details class="answer-drawer">
<summary>答案与解析</summary>

给出的选项中最准确的是：

```text
[C] 用来存放函数的局部变量，参数和返回地址的一片区域
```

更完整说法：stack frame/activation record 是一次函数调用在运行时的记录，通常包含参数、返回地址、old FP、static/dynamic link、saved registers、局部变量和 outgoing arguments。

判断 stack frame 选项时，关键词是“一次函数调用的运行时记录”。它不是整个程序的栈，也不是只存局部变量。

典型 frame 内容：

```text
incoming arguments
return address
old frame pointer
static link / dynamic link
saved registers
local variables that escape
spill slots
outgoing argument area
```

所以“存放函数的局部变量、参数和返回地址的一片区域”虽然不完整，但比只说“局部变量区域”更准确。

</details>

## 大题一：`a(a|b)*b(aa|bb)`

**题面：** 对正则表达式 `a(a|b)*b(aa|bb)` 完成 NFA、子集构造 DFA 和最小化说明。

<details class="answer-drawer">
<summary>答案与解析</summary>

语言含义：

```text
以 a 开头，并且以 baa 或 bbb 结尾的 a/b 串
```

因为中间 `(a|b)*` 可匹配任意 a/b 串，最后必须接：

```text
b aa
或
b bb
```

先把正则断成阶段：

```text
a          必须先读一个 a
(a|b)*     中间任意多个 a/b
b          猜测“这里是最后三位的第一位”
(aa|bb)    最后两位要么 aa，要么 bb
```

因此字符串必须满足：

```text
1. 第一个字符是 a。
2. 最后三个字符是 baa 或 bbb。
3. 中间部分可以是任意 a/b 串。
```

### 1. NFA 画法

若不强制 Thompson，可以按阶段画：

<figure class="svg-diagram" style="--diagram-max: 820px; --diagram-min: 620px;">
  <svg class="compiler-diagram" viewBox="0 0 820 330" role="img" aria-labelledby="memory-nfa-title">
    <title id="memory-nfa-title">回忆卷大题一的 NFA 画法</title>
    <defs>
      <marker id="memory-nfa-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 Z" fill="#607476" />
      </marker>
    </defs>
    <circle class="node-soft" cx="70" cy="165" r="28" />
    <text x="70" y="170" text-anchor="middle">start</text>
    <circle class="node" cx="205" cy="165" r="28" />
    <text x="205" y="170" text-anchor="middle">q1</text>
    <circle class="node" cx="365" cy="165" r="28" />
    <text x="365" y="170" text-anchor="middle">q2</text>
    <circle class="node" cx="525" cy="95" r="28" />
    <text x="525" y="100" text-anchor="middle">q3</text>
    <circle class="node" cx="525" cy="235" r="28" />
    <text x="525" y="240" text-anchor="middle">q4</text>
    <circle class="node" cx="700" cy="95" r="28" />
    <circle fill="none" stroke="#89a8a4" stroke-width="1.5" cx="700" cy="95" r="22" />
    <text x="700" y="100" text-anchor="middle">final1</text>
    <circle class="node" cx="700" cy="235" r="28" />
    <circle fill="none" stroke="#89a8a4" stroke-width="1.5" cx="700" cy="235" r="22" />
    <text x="700" y="240" text-anchor="middle">final2</text>
    <path class="edge" marker-end="url(#memory-nfa-arrow)" d="M98 165 H171" />
    <text x="136" y="152" text-anchor="middle" class="label">a</text>
    <path class="edge" marker-end="url(#memory-nfa-arrow)" d="M233 165 H331" />
    <text x="282" y="152" text-anchor="middle" class="label">b</text>
    <path class="edge" marker-end="url(#memory-nfa-arrow)" d="M386 148 C424 119 457 103 493 98" />
    <text x="443" y="114" text-anchor="middle" class="label">a</text>
    <path class="edge" marker-end="url(#memory-nfa-arrow)" d="M386 182 C424 211 457 227 493 232" />
    <text x="443" y="221" text-anchor="middle" class="label">b</text>
    <path class="edge" marker-end="url(#memory-nfa-arrow)" d="M553 95 H666" />
    <text x="610" y="82" text-anchor="middle" class="label">a</text>
    <path class="edge" marker-end="url(#memory-nfa-arrow)" d="M553 235 H666" />
    <text x="610" y="222" text-anchor="middle" class="label">b</text>
    <path class="edge" marker-end="url(#memory-nfa-arrow)" d="M211 137 C220 73 192 54 160 88 C132 118 153 143 177 153" />
    <text x="184" y="58" text-anchor="middle" class="label">a / b loop</text>
  </svg>
</figure>

注意这个 NFA 允许在任意位置猜“这里是最后的 b”，因此 $q_1 \xrightarrow{b} q_2$ 与 $q_1 \xrightarrow{b} q_1$ 可以同时存在，这正是 NFA 的非确定性。

用表格写 NFA 更适合考试：

| NFA 状态 | 读 `a` | 读 `b` | 说明 |
|---|---|---|---|
| `start` | `{q1}` | `∅` | 必须以 `a` 开头 |
| `q1` | `{q1}` | `{q1,q2}` | `(a|b)*` 循环；读 `b` 时也可猜 suffix 开始 |
| `q2` | `{q3}` | `{q4}` | 进入最后 `(aa|bb)` 的分支 |
| `q3` | `{final1}` | `∅` | 接受 suffix `baa` |
| `q4` | `∅` | `{final2}` | 接受 suffix `bbb` |
| `final1` | `∅` | `∅` | 终态 |
| `final2` | `∅` | `∅` | 终态 |

这个 NFA 没有 ε 边，所以后面子集构造时：

```text
ε-closure(T) = T
```

### 2. 子集构造思路

从 `{start}` 开始。每个 DFA 状态都是一组 NFA 状态：

| DFA 状态 | NFA 状态集合 | 读 `a` 后 | 读 `b` 后 | 是否终态 |
|---|---|---|---|---|
| `D0` | `{start}` | `D1` | `Dead` | 否 |
| `D1` | `{q1}` | `D1` | `D2` | 否 |
| `D2` | `{q1,q2}` | `D3` | `D4` | 否 |
| `D3` | `{q1,q3}` | `D5` | `D2` | 否 |
| `D4` | `{q1,q2,q4}` | `D3` | `D6` | 否 |
| `D5` | `{q1,final1}` | `D1` | `D2` | 是 |
| `D6` | `{q1,q2,q4,final2}` | `D3` | `D6` | 是 |
| `Dead` | `∅` | `Dead` | `Dead` | 否 |

其中几行的计算过程：

```text
move({q1,q2}, a)
  = move(q1,a) ∪ move(q2,a)
  = {q1} ∪ {q3}
  = {q1,q3} = D3

move({q1,q2,q4}, b)
  = move(q1,b) ∪ move(q2,b) ∪ move(q4,b)
  = {q1,q2} ∪ {q4} ∪ {final2}
  = {q1,q2,q4,final2} = D6
```

包含 `final1` 或 `final2` 的集合就是 DFA 终态，所以 `D5`、`D6` 是终态。

### 3. 最小化

按 DFA 最小化的 partition refinement 写。初始划分：

```text
P0 =
  F  = {D5,D6}
  NF = {D0,D1,D2,D3,D4,Dead}
```

第一轮按“读 `a/b` 后落入 F 还是 NF”拆：

```text
D3: a -> F,  b -> NF
D4: a -> NF, b -> F
D0,D1,D2,Dead: a -> NF, b -> NF
D5: a -> NF, b -> NF
D6: a -> NF, b -> F
```

因此得到：

```text
{D0,D1,D2,Dead} | {D3} | {D4} | {D5} | {D6}
```

继续细分 `{D0,D1,D2,Dead}`：

```text
D2: a -> {D3}, b -> {D4}
D0,D1,Dead: 读 a/b 都还落在 {D0,D1,Dead} 这一大组
```

所以拆成：

```text
{D0,D1,Dead} | {D2} | {D3} | {D4} | {D5} | {D6}
```

再细分 `{D0,D1,Dead}`：

```text
D1: b -> D2
D0: b -> Dead
Dead: b -> Dead
```

所以 `D1` 单独拆出。最后 `D0` 和 `Dead` 也会拆开，因为：

```text
D0: a -> D1
Dead: a -> Dead
```

最终各状态都不可合并，若保留 dead state，最小 DFA 仍有：

```text
D0,D1,D2,D3,D4,D5,D6,Dead
```

若考试允许画 partial DFA，可以省略 `Dead`，但要说明未定义转移都进入死状态。

检查点：

- 每个 DFA 状态必须是 NFA 状态集合。
- 终态判断必须看集合中是否含 NFA final。
- 最小化必须写每轮划分依据。

</details>

## 大题二：LR(1) 文法 `S → CC, C → aC | b`

**题面：** 对文法 `S → C C, C → a C | b` 构造 canonical LR(1) 项集族、状态转移和 parsing table；把 `$` 看作 EOF。

<details class="answer-drawer">
<summary>答案与解析</summary>

文法：

```text
0. S' → S $
1. S  → C C
2. C  → a C
3. C  → b
```

把 `$` 看作 EOF。Canonical LR(1) 项集可写为：

LR(1) closure 规则先写出来：

```text
若 [A → α . B β, a] 在 I 中，
对 B 的每条产生式 B → γ，
对每个 b ∈ FIRST(βa)，
把 [B → . γ, b] 加入 I。
```

本题的 FIRST 很简单：

```text
FIRST(a C) = {a}
FIRST(b)   = {b}
FIRST(C)   = {a,b}
FIRST($)   = {$}
```

构造 `I0` 的过程：

```text
起始项:
  [S' → . S $, $]

点后是 S，β 是 $，lookahead 是 $:
  FIRST($ $) = {$}
加入:
  [S → . C C, $]

现在点后是 C，β 是 C，lookahead 是 $:
  FIRST(C $) = FIRST(C) = {a,b}
加入:
  [C → . a C, a]
  [C → . a C, b]
  [C → . b, a]
  [C → . b, b]
```

所以 `I0` 是：

```text
I0:
  [S' → . S $, $]
  [S  → . C C, $]
  [C  → . a C, a]
  [C  → . a C, b]
  [C  → . b, a]
  [C  → . b, b]

I1 = goto(I0,S):
  [S' → S . $, $]

I2 = goto(I0,C):
  [S → C . C, $]
  [C → . a C, $]
  [C → . b, $]

I3 = goto(I0,a):
  [C → a . C, a]
  [C → a . C, b]
  [C → . a C, a]
  [C → . a C, b]
  [C → . b, a]
  [C → . b, b]

I4 = goto(I0,b):
  [C → b ., a]
  [C → b ., b]

I5 = goto(I1,$):
  [S' → S $ ., $]

I6 = goto(I2,C):
  [S → C C ., $]

I7 = goto(I2,a):
  [C → a . C, $]
  [C → . a C, $]
  [C → . b, $]

I8 = goto(I2,b):
  [C → b ., $]

I9 = goto(I3,C):
  [C → a C ., a]
  [C → a C ., b]

I10 = goto(I7,C):
  [C → a C ., $]
```

几个关键 `goto` 展开如下。

`goto(I0, C)`：

```text
从 I0 中找点后为 C 的 item:
  [S → . C C, $]

移动点:
  [S → C . C, $]

点后仍是 C，β 为空，lookahead 是 $:
  FIRST($) = {$}
加入:
  [C → . a C, $]
  [C → . b, $]

得到 I2。
```

`goto(I0, a)`：

```text
从 I0 中找点后为 a 的 item:
  [C → . a C, a]
  [C → . a C, b]

移动点:
  [C → a . C, a]
  [C → a . C, b]

对第一个 item，点后 C，β 为空，lookahead a:
  FIRST(a) = {a}
加入 C 的 a-lookahead 产生式。

对第二个 item，lookahead b:
  FIRST(b) = {b}
加入 C 的 b-lookahead 产生式。

得到 I3。
```

`goto(I2, a)`：

```text
I2 中点后为 a 的 item:
  [C → . a C, $]

移动点:
  [C → a . C, $]

点后 C，β 为空，lookahead $:
  FIRST($) = {$}
加入:
  [C → . a C, $]
  [C → . b, $]

得到 I7。
```

循环边：

<figure class="svg-diagram" style="--diagram-max: 720px; --diagram-min: 520px;">
  <svg class="compiler-diagram" viewBox="0 0 720 280" role="img" aria-labelledby="lr1-loop-edges-title">
    <title id="lr1-loop-edges-title">LR(1) 项集中的循环边</title>
    <defs>
      <marker id="lr1-loop-edges-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 Z" fill="#607476" />
      </marker>
    </defs>
    <circle class="node-soft" cx="170" cy="90" r="30" />
    <text x="170" y="95" text-anchor="middle" class="title">I3</text>
    <circle class="node" cx="390" cy="90" r="30" />
    <text x="390" y="95" text-anchor="middle">I4</text>
    <circle class="node-soft" cx="170" cy="200" r="30" />
    <text x="170" y="205" text-anchor="middle" class="title">I7</text>
    <circle class="node" cx="390" cy="200" r="30" />
    <text x="390" y="205" text-anchor="middle">I8</text>
    <path class="edge" marker-end="url(#lr1-loop-edges-arrow)" d="M198 90 H356" />
    <text x="278" y="77" text-anchor="middle" class="label">b</text>
    <path class="edge" marker-end="url(#lr1-loop-edges-arrow)" d="M198 200 H356" />
    <text x="278" y="187" text-anchor="middle" class="label">b</text>
    <path class="edge" marker-end="url(#lr1-loop-edges-arrow)" d="M170 60 C212 20 128 20 170 60" />
    <text x="170" y="25" text-anchor="middle" class="label">a</text>
    <path class="edge" marker-end="url(#lr1-loop-edges-arrow)" d="M170 170 C212 130 128 130 170 170" />
    <text x="170" y="135" text-anchor="middle" class="label">a</text>
  </svg>
</figure>

Parsing table 填法：

- `I0` on `a` shift `I3`，on `b` shift `I4`，GOTO `S/I1`，`C/I2`。
- `I1` on `$` shift `I5`。
- `I2` on `a` shift `I7`，on `b` shift `I8`，GOTO `C/I6`。
- `I3` on `a` shift `I3`，on `b` shift `I4`，GOTO `C/I9`。
- `I4` on `a,b` reduce `C->b`。
- `I5` accept。
- `I6` on `$` reduce `S->CC`。
- `I7` on `a` shift `I7`，on `b` shift `I8`，GOTO `C/I10`。
- `I8` on `$` reduce `C->b`。
- `I9` on `a,b` reduce `C->aC`。
- `I10` on `$` reduce `C->aC`。

如果写成表，ACTION/GOTO 是：

| 状态 | `a` | `b` | `$` | GOTO `S` | GOTO `C` |
|---|---|---|---|---|---|
| `I0` | s3 | s4 |  | I1 | I2 |
| `I1` |  |  | s5 |  |  |
| `I2` | s7 | s8 |  |  | I6 |
| `I3` | s3 | s4 |  |  | I9 |
| `I4` | r3 | r3 |  |  |  |
| `I5` |  |  | accept |  |  |
| `I6` |  |  | r1 |  |  |
| `I7` | s7 | s8 |  |  | I10 |
| `I8` |  |  | r3 |  |  |
| `I9` | r2 | r2 |  |  |  |
| `I10` |  |  | r2 |  |  |

产生式编号对应：

```text
r1: S → C C
r2: C → a C
r3: C → b
```

检查点：

- `I4` 的完整项 `[C → b ., a]` 和 `[C → b ., b]` 只在 `a,b` 上 reduce，不在 `$` 上 reduce。
- `I8` 的 `[C → b ., $]` 只在 `$` 上 reduce。
- 这正是 LR(1) 比 SLR 精确的地方：reduce 列由 item 自带 lookahead 决定。

</details>

## 大题三：Tiger 语义环境

**题面：** 给定 Tiger 程序，列出各层 TEnv/VEnv 变化，判断变量绑定和 type-check 是否成功。

```tiger
let
  type arr = array of int
  var x := 10
  function f(x:int):int =
    let
      var a: arr := arr[5] of x
      function g(): int = x + a[0]
    in
      g()
    end
in
  f(3)
end
```

<details class="answer-drawer">
<summary>答案与解析</summary>

### Global / outer let scope

基础环境已有：

```text
TEnv: int, string
VEnv: print, flush, getchar, ...
```

外层 `let` 新增：

```text
TEnv: arr → array of int
VEnv: x → int
VEnv: f → function(int) → int
```

处理外层 `let` 的顺序：

```text
1. type arr = array of int
   在 TEnv 中加入 arr。

2. var x := 10
   10 : int，所以在 VEnv 中加入 x:int。

3. function f(x:int):int = ...
   先把函数 header 加入 VEnv：
   f : function(int) -> int
   再进入函数体检查。
```

如果有多条互递归 function declaration，Tiger 通常先收集 header，再检查 body；本题只有一个 `f`，但写答案时仍可按“先登记函数类型，再检查函数体”的思路。

### Entering parameter scope of function `f`

进入 `f` 函数体时新开作用域：

```text
VEnv: x → int     // parameter x，shadow outer var x
TEnv: arr → array of int  // 从外层可见
```

`f` 自身和外层 `x` 仍在外层环境中，但名字 `x` 查找会先找到参数 `x`。

这里的 shadowing 是得分点：

```text
outer x : int     // var x := 10
parameter x : int // function f(x:int)
```

在 `f` 体内查找 `x` 时，先查最近作用域，所以得到 parameter `x`，不是 outer `x`。

### Inside let block within `f`

处理：

```text
var a: arr := arr[5] of x
function g(): int = x + a[0]
```

新增：

```text
VEnv: a → arr
VEnv: g → function() → int
TEnv: 无新增类型
```

处理 `a` 的 initializer：

```text
arr[5] of x
```

检查步骤：

```text
1. 查 TEnv: arr 是 array of int。
2. 检查 size 表达式 5 : int。
3. 检查 init 表达式 x : int。
4. init 类型 int 与 arr 元素类型 int 一致。
5. 所以 arr[5] of x : arr。
```

然后把：

```text
a : arr
```

加入 `f` 内部 let 的 VEnv。

处理 `g` 时同样先登记函数 header：

```text
g : function() -> int
```

再检查 `g` 的 body。

### `g` 内部的引用

```text
x + a[0]
```

- `x` 绑定到 `f` 的参数 `x:int`，不是外层 `var x := 10`。
- `a[0]` 中 `a` 绑定到 `f` 内部 let 的局部变量 `a:arr`。
- `a[0]` 类型是 `int`。

### Type-check 是否成功

成功。理由：

```text
arr[5] of x: size 5 是 int，init x 是 int，得到 arr
g(): x:int + a[0]:int，返回 int
f(x:int):int 的 body 是 g():int
外层 f(3): 3:int，调用返回 int
```

按表达式从内到外写：

```text
g() : int
f(3) : int
整个 outer let 的 in 部分类型是 int
```

本题常见扣分点：

- 把 `g` 里的 `x` 错绑到外层 `var x := 10`。
- 忘记 `arr[5] of x` 的 `x` 是 `f` 的参数。
- 把 `a[0]` 的类型写成 `arr`；实际数组下标表达式结果是元素类型 `int`。

</details>

## 大题四：IR Tree

**题面：** 写出 `a[i+2]` 的 Tree IR，并把条件赋值翻译成短路控制流 IR。已知 `a` 在栈上相对 `fp` 的 offset 为 20 bytes。

```text
if h < 9 or h > 21:
  off_work := 1
else:
  off_work := 0
```

<details class="answer-drawer">
<summary>答案与解析</summary>

### `a[i+2]`

若 `a` 是数组指针变量，先从 frame 取出数组基地址：

```text
a_ptr = MEM(BINOP(PLUS, TEMP fp, CONST 20))
index = BINOP(PLUS, TEMP i, CONST 2)
addr  = BINOP(PLUS, a_ptr, BINOP(MUL, index, CONST W))
value = MEM(addr)
```

合成 Tree IR：

```text
MEM(
  BINOP(PLUS,
    MEM(BINOP(PLUS, TEMP fp, CONST 20)),
    BINOP(MUL,
      BINOP(PLUS, TEMP i, CONST 2),
      CONST W)))
```

若 `W` 表示 word 数而不是 byte 数，还要乘以 `wordSize`。

逐步推导：

```text
1. 局部变量 a 在 frame 中，地址是 fp + 20。
2. 因为 a 是数组指针变量，frame slot 里存的是数组基地址：
   MEM(fp + 20)
3. 下标是 i + 2。
4. 字节偏移是 (i + 2) * W。
5. 元素地址是 base + offset。
6. 取元素值要再套一层 MEM。
```

如果题目把 `a` 设为“数组对象本身直接从 fp+20 开始”，那就不需要第一层取指针：

```text
MEM(BINOP(PLUS,
  BINOP(PLUS, TEMP fp, CONST 20),
  BINOP(MUL, BINOP(PLUS, TEMP i, CONST 2), CONST W)))
```

但 Tiger/虎书语境里 array/record 变量通常是 pointer，所以主答案使用 `MEM(fp+20)` 取数组基址。

### 条件赋值

短路 OR 的 IR：

```text
CJUMP(LT, TEMP h, CONST 9, Ltrue, Lcheck2)
LABEL Lcheck2
CJUMP(GT, TEMP h, CONST 21, Ltrue, Lfalse)

LABEL Ltrue
MOVE(TEMP off_work, CONST 1)
JUMP(NAME Ldone)

LABEL Lfalse
MOVE(TEMP off_work, CONST 0)

LABEL Ldone
```

关键：`or` 不是先算两个布尔值再相加，而是第一个条件为真时直接跳到 true 分支。

控制流检查：

```text
h < 9 为真:
  第一条 CJUMP 直接到 Ltrue，off_work := 1。

h < 9 为假:
  到 Lcheck2，继续检查 h > 21。

h > 21 为真:
  到 Ltrue，off_work := 1。

两个条件都假:
  到 Lfalse，off_work := 0。
```

`Ltrue` 分支后必须 `JUMP Ldone`，否则会 fall-through 到 `Lfalse`，把 `off_work` 又改成 0。

</details>

## 大题五：Basic Blocks 与 Longest Trace

**题面：** 将给定控制流代码划分 basic blocks，补齐 fall-through label，并给出一种合法 longest trace。

<details class="answer-drawer">
<summary>答案与解析</summary>

原代码需要给 fall-through 位置补 label：

```text
B0:
  LABEL L0
  c <- a+b
  CJUMP(d<2, L4, L0_false)

B1:
  LABEL L0_false
  c <- a*b
  JUMP L1

B2:
  LABEL L1
  a <- b%2
  JUMP L4

B3:
  LABEL L2
  c <- d*a
  CJUMP(c>0, L1, L2_false1)

B4:
  LABEL L2_false1
  n <- n-2
  CJUMP(n<0, L3, L2_false2)

B5:
  LABEL L2_false2
  a <- a+1
  JUMP L4

B6:
  LABEL L3
  b <- a+2
  JUMP L4

B7:
  LABEL L4
  d <- c%2
  n <- n+2
  JUMP done
```

一种 longest trace：

```text
L0 → L0_false → L1 → L4
```

也可以给：

```text
L2 → L2_false1 → L2_false2 → L4
```

只要每个 trace 内控制流可连续，并且最终所有 basic blocks 被 trace covering 覆盖即可。

作答步骤模板：

```text
1. 每个 basic block 必须以 LABEL 开头。
2. 每个 block 必须以 JUMP/CJUMP 结尾。
3. 如果 CJUMP 的 false 分支原本是“下一条语句”，要补一个新 label。
4. 如果普通语句后面直接接 LABEL，要在前一个 block 末尾补 JUMP。
5. trace covering 时，每个 block 只能出现在一个 trace 里。
```

以上划分中：

```text
B0 的 CJUMP false 分支补成 L0_false。
B3 的 CJUMP false 分支补成 L2_false1。
B4 的 CJUMP false 分支补成 L2_false2。
```

选择 `L0 → L0_false → L1 → L4` 作为 trace 的理由：

```text
L0 false 分支自然落到 L0_false
L0_false 最后 JUMP L1
L1 最后 JUMP L4
```

这是连续可走的一条路径，所以是合法 trace。若题目要求 longest trace，可以从所有未访问 block 中贪心选最长后继链，直到覆盖完全部 block。

</details>

## 大题六：Liveness 与 Interference

**题面：** 根据题目给出的代码/live-out 信息完成 liveness analysis，并构造 interference graph。

<details class="answer-drawer">
<summary>答案与解析</summary>

回忆卷没有给出完整代码和 live-out 提示，不能唯一计算。标准作答格式：

```text
1. 给每条 statement 编号。
2. 写 use/def。
3. 写 succ。
4. 从最后一条开始反向迭代：
   out[n] = ⋃_{s ∈ succ[n]} in[s]
   in[n] = use[n] ∪ (out[n] - def[n])
5. 对每条定义 d，把 d 与 live-out[n] 中除自身外的变量连边。
6. move d<-s 特殊：通常不连 d-s，记录 coalescing candidate。
```

考试如果给了部分 live-out，可以用它校验自己迭代结果。

详细写法可以按表格：

| n | statement | use[n] | def[n] | succ[n] | in[n] | out[n] |
|---|---|---|---|---|---|---|
| 1 | ... | ... | ... | ... | ... | ... |

迭代顺序通常从最后一条往前更快收敛：

```text
repeat
  for n in reverse order:
    old_in  = in[n]
    old_out = out[n]
    out[n] = union(in[s] for s in succ[n])
    in[n]  = use[n] union (out[n] - def[n])
until no in/out changes
```

干涉边构造：

```text
对普通定义 d:
  for each x in out[n]:
    if x != d:
      add edge d-x

对 move d <- s:
  for each x in out[n] - {s,d}:
    add edge d-x
  record move (d,s) for possible coalescing
```

小例子：

```text
1: a := 1
2: b := a + 1
3: c := b + a
4: return c
```

计算得到：

| 行 | use | def | in | out |
|---|---|---|---|---|
| 4 | `{c}` | `{}` | `{c}` | `{}` |
| 3 | `{a,b}` | `{c}` | `{a,b}` | `{c}` |
| 2 | `{a}` | `{b}` | `{a}` | `{a,b}` |
| 1 | `{}` | `{a}` | `{}` | `{a}` |

干涉边至少有：

```text
b -- a
```

因为第 2 行定义 `b`，而 `a` 在该行 out 中仍 live。

</details>

## 大题七：Spill 与图着色

**题面：** 根据题目给出的干涉图和 live-out 信息计算 spill priority，执行 spill rewrite、重新建图、coalescing 和最终寄存器分配。

<details class="answer-drawer">
<summary>答案与解析</summary>

题目缺原始冲突图和部分 live-out，无法唯一完成数值答案。以下是标准解题流程。

### 1. Spill Priority

常见公式：

```text
spill priority = use frequency / degree
```

例子：

| temp | 循环外 use/def | 循环内 use/def | 加权频率 | degree | priority |
|---|---:|---:|---:|---:|---:|
| `a` | 1 | 2 | `1+2n` | 5 | `(1+2n)/5` |
| `b` | 3 | 0 | `3` | 2 | `3/2` |

若 `n=10`：

```text
priority(a) = 21/5 = 4.2
priority(b) = 3/2 = 1.5
```

priority 较低的 `b` 更适合作为 spill candidate。

若循环平均执行 `n` 次，循环内使用/定义的变量频率要乘 `n`；循环外只算 1 次。

步骤：

```text
1. 对每个 temporary 统计 uses + defs。
2. 循环内出现的乘以 n。
3. 查原始干涉图 degree。
4. priority 越低，越适合 spill。
```

### 2. Spill Rewrite

若选择 spill `v` 到 frame slot `slot_v`：

```text
每次 use v 前:
  v_use_i <- MEM[slot_v]
  用 v_use_i 替代 v

每次 def v 后:
  v_def_i <- ...
  MEM[slot_v] <- v_def_i
```

新增 temporary 的 live range 通常很短。

更具体地说：

```text
原来:
  t <- v + 1
  v <- t * 2

spill v 后:
  v_load1 <- MEM[slot_v]
  t <- v_load1 + 1

  v_def1 <- t * 2
  MEM[slot_v] <- v_def1
```

每次 use 前插入 load，每次 def 后插入 store。不要复用同一个新 temporary 跨很长范围，否则会重新制造长 live range。

### 3. New Interference Graph

重写后重新做 liveness。不能在旧图上简单删掉 `v`，因为新增 load/store 和 temporaries 会改变 live ranges。

重建流程：

```text
rewritten program
→ rebuild flow graph
→ recompute use/def
→ rerun liveness
→ rebuild interference graph
→ rerun coloring
```

如果新临时变量只在 load 后一两条语句内使用，通常度数较低，容易被分配到寄存器。

### 4. Coalescing

- 与 precolored node 合并：用 George criterion。
- 普通 temporary 合并：用 Briggs criterion。

作答时要写：

```text
当前 worklists
Simplify 删除了哪些节点
Coalesce 合并了哪些 move
Freeze 了哪些 move
Select spill 选择了谁
selectStack
最终颜色
```

简化版流程：

```text
Build:
  建干涉图和 move list。

Simplify:
  删除 degree < K 且 non-move-related 的节点，压栈。

Coalesce:
  对 move x <- y，如果保守判据允许，合并 x/y。

Freeze:
  如果低度 move-related 节点阻塞流程，冻结相关 move。

SelectSpill:
  如果没有低度节点，按 spill priority 选 candidate。

AssignColors:
  从 selectStack 弹出节点，选一个不与已着色邻居冲突的颜色。

Rewrite:
  若某节点无色可选，才是 actual spill，插入 load/store 后重新开始。
```

### 5. Rewrite Final Program

最终程序要：

- 删除成功 coalesced 的 move。
- 替换 temporary 为物理寄存器。
- 保留 spill 引入的 load/store。
- 满足 calling convention，例如 call 使用 `r1/r2`，返回定义 `r1`。

这题分值高，主要给步骤分；没有图时无法补全唯一答案。

最终程序检查清单：

```text
1. 每个 temporary 要么换成物理寄存器，要么已被 spill load/store 替代。
2. 成功 coalesced 的 move 删除。
3. 未 coalesced 的 move 若源/目的寄存器不同，仍保留。
4. call 前后满足 calling convention。
5. spill slot 只通过 load/store 访问，不再直接把 spilled temp 当寄存器用。
```

</details>
