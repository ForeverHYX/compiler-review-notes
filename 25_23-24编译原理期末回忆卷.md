# 23-24 编译原理期末回忆卷（小题 + 大题补全版）
Date: 2024-06-20
Author: Yixun Hong
Tags: Compiler, Final, Recall, Tiger
Abstract: 根据 23-24 学年编译原理回忆卷 PDF 的小题部分整理，并按回忆补全大题题干。PDF 中缺失的单选题题干或选项已按课堂常见考点补全，便于完整自测。

## 使用说明

- 判断题与选择题来自回忆 PDF；原 PDF 标注“忘了”的位置已按知识点补全。
- 大题根据回忆点补成可做题目，答案给的是参考步骤，不是唯一写法。
- 每题下方都有折叠答案；先做完再展开。

## 一、判断题

1. 将有 n 个状态的 NFA 通过子集构造的方法转为 DFA 时，构造出的 DFA 最多有 2^n 个状态。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>对。子集构造的 DFA 状态是 NFA 状态集合的子集，最多有 2^n 个。</p>
</details>

2. 正则表达式 `"/*" "/"* ( [^*/] | [^*]"/" | "*"[^/] )* "*"* "*/"` 可以识别所有 C 风格注释。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>错。C 风格注释要求从 /* 到第一个 */ 结束，不能简单认为该表达式覆盖所有边界情况；课堂中常强调注释识别要避免把中间的 */ 吞掉。</p>
</details>

3. 上下文无关文法 `S -> (S)S | ε` 是 LL(1) 的。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>对。`FIRST((S)S) = {(}`，`FIRST(ε) = {ε}`，且 `FOLLOW(S)` 中的符号与 `(` 不冲突，可以用一个向前看符号预测。</p>
</details>

4. 一般情况下，不考虑使用散列表作为命令式符号表的实现。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>错。命令式符号表常用散列表配合作用域栈实现；函数式符号表更偏向持久化树结构。</p>
</details>

5. 嵌套函数用于访问外层函数局部变量的 static link 在运行时指向其调用者的栈帧。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>错。static link 指向词法外层函数的栈帧，不一定是调用者；dynamic link 才反映调用者。</p>
</details>

6. 访问 escaping variable 时，应当在寄存器中寻找。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>错。escaping variable 需要放在栈帧中，供内部函数或越过作用域的访问使用。</p>
</details>

7. display 是一个维护 fp 的数组。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>对。display 通常按词法层级保存对应活动记录的 frame pointer，用于快速访问外层变量。</p>
</details>

8. 语法分析、词法分析、语义分析是编译器前端的任务。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>对。前端负责从源程序到抽象语法树/中间表示前的分析工作。</p>
</details>

9. 指令选择的任务是根据中间代码生成有最少指令数的机器指令序列。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>错。指令选择通常优化代价，不一定等价于最少指令数；不同指令代价、寻址模式和后续寄存器分配都会影响选择。</p>
</details>

10. maximal munch 算法可以产生 optimum tiling。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>错。maximal munch 是贪心算法，通常快但不保证全局最优；动态规划才用于 optimum tiling。</p>
</details>

11. 进行变量活性分析时，需要以从未来到过去的方向进行分析。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>对。活跃性是后向数据流问题，`in[n]` 依赖 `out[n]`，`out[n]` 依赖后继的 `in`。</p>
</details>

12. 数据流分析时，数据流方程的解是一个保守估计。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>对。编译器宁可多认为一些变量活跃/可达，也不能漏掉真实情况。</p>
</details>

13. 将中间代码树转化为正则树时会将所有 CALL 指令消去。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>错。规范化会把 CALL 移到安全位置，例如包进 ESEQ/MOVE 临时量，但 CALL 本身仍然存在。</p>
</details>

14. 节点 d 支配节点 n 的充分条件是，某一条从入口 s0 到 n 的路径经过 d。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>错。支配要求所有从入口到 n 的路径都经过 d，而不是存在一条路径。</p>
</details>

15. 当 x != z 时，`MOVE(MEM(x), y)` 和 `MEM(z)` 是可交换的。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>通常按课堂简化假设为对。若 x 与 z 表示不同地址，写 MEM(x) 不影响读 MEM(z)。真实编译器还要考虑别名分析。</p>
</details>

16. 若 e1 与 s 可交换，则 `[e1, e2, ESEQ(s, e3)]` 与 `[SEQ(MOVE(t, e1), s), t, e2, e3]` 等价。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>错。规范化列表时若 e1 与 s 可交换，可以保持 e1 在前；若不可交换才需要引入临时量保存 e1。题中把条件和变换方向混淆了。</p>
</details>

17. 在依照含 move-related node 的相干图进行寄存器分配时，当简化和合并都无法进行时，应进行 spill 操作。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>错。典型算法还会先 freeze move-related node，冻结后再继续 simplify；spill 是更靠后的选择。</p>
</details>

18. 在含 move-related node 的相干图中，若 `(a,b)` 是 MOVE 边，且对每个 a 的相邻节点 t，t 与 b 相邻或 t 为不重要节点，则 a 与 b 可以合并。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>对。这是 George 合并准则的表述之一。</p>
</details>

19. Mark & Sweep 算法无法回收引用计数等于 0 的对象。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>错。引用计数为 0 的对象不可达，mark-sweep 可以回收；mark-sweep 的优势之一是能处理循环引用。</p>
</details>

20. BFS 比 DFS 造成更多的 cache miss。

<details class="answer-drawer">
<summary>参考答案</summary>
<p>一般按课堂讨论为对。BFS 的访问前沿更分散，局部性通常弱于沿路径深入的 DFS；具体仍依赖图存储布局。</p>
</details>

### 判断题解析速查

| 题号 | 关键判断依据 |
|---:|---|
| 1 | 子集构造的 DFA 状态是 NFA 状态集合，`n` 个 NFA 状态最多有 `2^n` 个子集。 |
| 2 | C 注释应在第一个 `*/` 结束；写 RE 时要特别检查是否会吞掉中间结束符或漏边界。 |
| 3 | `S -> (S)S | ε` 中非 ε 分支 FIRST 为 `(`，ε 分支看 FOLLOW，不与 `(` 冲突，因此可 LL(1)。 |
| 4 | 命令式符号表常用 hash table + scope marker/undo stack；函数式符号表才强调持久化旧版本。 |
| 5 | static link 指向词法外层 frame；dynamic link 才指向调用者 frame。 |
| 6 | escaping variable 需要稳定 frame slot，不能只存在寄存器。 |
| 7 | display 是按词法层级保存 frame pointer 的数组，可 O(1) 访问外层 frame。 |
| 8 | lexer/parser/semantic analysis 属于 front end；instruction selection/register allocation 属于 back end。 |
| 9 | 指令选择目标是用 machine tiles 覆盖 IR tree，优化 cost，不等于最少指令条数。 |
| 10 | maximal munch 不保证全局最低 cost；`optimum tiling` 要 dynamic programming。 |
| 11 | liveness 是 backward dataflow：当前是否活跃取决于未来是否会用。 |
| 12 | 数据流分析一般给保守近似，不能漏掉真实可能发生的情况。 |
| 13 | canonicalization 会提升/规整 `CALL`，不会消灭所有 `CALL`。 |
| 14 | dominator 定义是所有入口到 `n` 的路径都经过 `d`，不是某一条路径。 |
| 15 | 课堂简化下不同内存地址读写可交换；真实编译器还需 alias analysis。 |
| 16 | 若可交换，不需要保存 `e1` 到临时量；不可交换时才引入临时量保证求值顺序。 |
| 17 | iterated register coalescing 流程中 simplify/coalesce 卡住后通常先 freeze，不是立刻 spill。 |
| 18 | George 准则：邻居要么与目标相邻，要么低度，从而保守保证合并安全。 |
| 19 | mark-sweep 基于 reachability，能回收 unreachable 对象，包括引用计数为 0 或循环垃圾。 |
| 20 | 课堂常用直觉：BFS 前沿分散，局部性通常差于 DFS；具体依赖对象布局。 |

## 二、单项选择题

1. 一个 DFA 的状态表示读入二进制串后对 m 的余数，起始状态和接受状态均为余数 0。若状态集合为 `{0,1,2}`，读入 `0` 时转到 `(2r) mod m`，读入 `1` 时转到 `(2r+1) mod m`，该 DFA 识别哪些字符串？

A. 所表示数字能被 2 整除的二进制串  
B. 所表示数字能被 3 整除的二进制串  
C. 所表示数字能被 5 整除的二进制串  
D. 所表示数字能被 7 整除的二进制串

<details class="answer-drawer">
<summary>参考答案</summary>
<p>B。三个余数状态且转移按二进制追加位更新余数，接受余数 0，即识别能被 3 整除的二进制串。</p>
</details>

2. 给定文法 `exp -> exp addop term | term; addop -> "+" | "-"; term -> term mulop factor | factor; mulop -> "*"; factor -> "(" exp ")" | NUM`，求非终结符 `factor` 的 FIRST 集和 FOLLOW 集。

A. FIRST = `{ "(", NUM }`，FOLLOW = `{ "*", "+", "-", ")", $ }`  
B. FIRST = `{ NUM }`，FOLLOW = `{ "+", "-", $ }`  
C. FIRST = `{ "(", NUM }`，FOLLOW = `{ "+", "-", ")" }`  
D. FIRST = `{ "(", ")" }`，FOLLOW = `{ "*", $ }`

<details class="answer-drawer">
<summary>参考答案</summary>
<p>A。`factor` 可推出括号表达式或 NUM。它出现在 `term -> term mulop factor` 和 `term -> factor`，因此后面可跟乘号、加减、右括号或输入结束。</p>
</details>

3. 给定 LR parsing table，对文法 `S -> (S)S | ε`，当栈为 `#0(2S3)4S5`、输入为 `$` 时，进行一步操作后的栈顶为哪一项？

A. `S5`  
B. `S3`  
C. `S2`  
D. `S1`

<details class="answer-drawer">
<summary>参考答案</summary>
<p>A。状态 5 遇到 `$` 按表对 `S -> (S)S` 规约，弹出右部对应符号后 goto 回到新的 S 状态；回忆表中该题选项的栈顶为 `S5`。</p>
</details>

4. 在 yacc 产生式 `exp: exp PLUS pos exp` 中，`pos` 的语义值用哪个符号表示？

A. `$4`  
B. `$3`  
C. `$2`  
D. `$1`

<details class="answer-drawer">
<summary>参考答案</summary>
<p>B。产生式右部从 1 开始计数：第 1 个 exp 是 `$1`，PLUS 是 `$2`，pos 是 `$3`，最后的 exp 是 `$4`。</p>
</details>

5. LR parsing 中不涉及以下哪个操作？

A. shift  
B. accept  
C. reduce  
D. match

<details class="answer-drawer">
<summary>参考答案</summary>
<p>D。LR parser 的动作是 shift/reduce/accept/error；match 是 LL 预测分析里常见的终结符匹配动作。</p>
</details>

6. 访问在内存中的局部变量，需要用其在栈帧中的偏移量加上哪个基址？

A. fp  
B. sp  
C. static link  
D. global pointer

<details class="answer-drawer">
<summary>参考答案</summary>
<p>A。Tiger/Appel 风格中帧内变量通常用 frame pointer 加偏移访问；sp 会随压栈变化。</p>
</details>

7. 在嵌套定义的函数中，内层函数访问外层函数局部变量主要依赖哪一项？

A. fp  
B. sp  
C. static link  
D. return address

<details class="answer-drawer">
<summary>参考答案</summary>
<p>C。static link 串起词法外层活动记录，用于访问非局部变量。</p>
</details>

8. 以下哪项内容可能出现在函数的栈帧中？

A. 保存的 fp  
B. sp 寄存器本身  
C. static variable 的存储本体  
D. global variable 的存储本体

<details class="answer-drawer">
<summary>参考答案</summary>
<p>A。栈帧中可保存旧 fp、返回地址、形参、逃逸局部变量、callee-save 寄存器等；静态/全局变量通常不在普通函数栈帧中。</p>
</details>

9. 函数式符号表通常使用哪种效率较高的数据结构？

A. 平衡二叉搜索树  
B. 线性数组  
C. 单链表  
D. 栈上临时数组

<details class="answer-drawer">
<summary>参考答案</summary>
<p>A。函数式符号表需要持久化和共享旧版本，平衡树更自然；命令式符号表常用散列表。</p>
</details>

10. Tiling 是将正则树覆盖为机器指令模板的过程，这一过程与以下哪项最无关？

A. 机器指令模板  
B. maximal munch  
C. 树文法  
D. 最终物理寄存器分配

<details class="answer-drawer">
<summary>参考答案</summary>
<p>D。tiling 属于指令选择；物理寄存器分配通常在其后处理，虽会影响最终代码质量，但不是 tiling 本身。</p>
</details>

11. 使用动态规划法对正则树分片时，不会使用以下哪种方法？

A. minimal munch  
B. bottom-up computing  
C. optimum solution  
D. tiny tiling

<details class="answer-drawer">
<summary>参考答案</summary>
<p>A。动态规划自底向上计算最优代价；minimal munch 不是该算法的标准组成。</p>
</details>

12. Tree Grammar 主要用于哪种算法/工具？

A. fast tiling  
B. maximal munch  
C. optimal tiling / code-generator generator  
D. lexical scanner

<details class="answer-drawer">
<summary>参考答案</summary>
<p>C。树文法描述 IR 树到指令的模板，可被代码生成器生成工具用于 optimal tiling。</p>
</details>

13. 活性分析中不需要直接用到以下哪一项？

A. use[n]  
B. def[n]  
C. pred[n]  
D. succ[n]

<details class="answer-drawer">
<summary>参考答案</summary>
<p>C。标准方程是 `in[n] = use[n] ∪ (out[n] - def[n])`，`out[n] = ∪ in[s]` for `s in succ[n]`，不直接使用 pred。</p>
</details>

14. 相干图（interference graph）的主要用途是什么？

A. 描述变量之间不能分配到同一寄存器的冲突关系  
B. 描述 CFG 中基本块之间的跳转关系  
C. 描述语法分析中的移进/规约冲突  
D. 描述对象之间的引用关系

<details class="answer-drawer">
<summary>参考答案</summary>
<p>A。相干图中相邻节点表示两个 temporaries 同时活跃，不能使用同一个寄存器。</p>
</details>

15. 以下哪个树是正则树（canonical tree）？

A. `BINOP(PLUS, TEMP a, CONST 1)`  
B. `MOVE(MEM(CALL(NAME f, args)), TEMP a)`  
C. `ESEQ(MOVE(TEMP t, CONST 1), TEMP t)`  
D. `SEQ(MOVE(TEMP a, CONST 1), MOVE(TEMP b, CONST 2))`

<details class="answer-drawer">
<summary>参考答案</summary>
<p>A。正则树中不应含 ESEQ/SEQ，CALL 也需要被提升到安全位置。纯 BINOP 表达式是合法正则树。</p>
</details>

16. 一段机器指令中有 k 个 CJUMP 指令（k >= 1），则该段代码至少有多少个基本块？

A. k  
B. k + 1  
C. 2k  
D. 2k + 1

<details class="answer-drawer">
<summary>参考答案</summary>
<p>B。每个条件跳转至少结束一个基本块，并产生后续入口；最少情况下 k 个条件跳转串接形成 k+1 个块。</p>
</details>

17. 以下哪个将树转为正则树的过程是正确的？

A. 将 `BINOP(op, ESEQ(s,e), r)` 变为 `ESEQ(s, BINOP(op,e,r))`，并继续处理交换性  
B. 保留 `CJUMP` 中的任意 `ESEQ`，因为 CJUMP 不是表达式  
C. 将 `ESEQ(s1, ESEQ(s2,e))` 原样保留到指令选择阶段  
D. 将所有 `JUMP` 删除，只保留 label

<details class="answer-drawer">
<summary>参考答案</summary>
<p>A。规范化的核心是把 ESEQ 中的语句向外提升，并在必要时引入临时量保持求值顺序。</p>
</details>

18. 一条机器指令 `d <- a1 + a2` 在循环体中，以下哪种情况可以断定 `ai` 是循环内不变的？

A. `ai` 是常量  
B. 能 reach 该语句的 `ai` 的定义均在循环外  
C. `ai` 的定义中只有一条语句可以 reach 该语句  
D. 以上都不是

<details class="answer-drawer">
<summary>参考答案</summary>
<p>A 与 B 都是循环不变的典型充分条件；若本题是单选，课堂常取“以上描述中最完整的正确项”为 B。C 不够，因为唯一 reaching definition 可能在循环内。</p>
</details>

19. 以下算法中，会被 external fragmentation 影响的是哪一个？

A. Mark & Sweep  
B. 引用计数  
C. Copying collection  
D. 以上都不是

<details class="answer-drawer">
<summary>参考答案</summary>
<p>A。非移动式 mark-sweep 可能产生外部碎片；copying collection 会压缩对象。</p>
</details>

20. 给定相干图：interference 边为 `(a,b), (a,c), (b,c), (b,d), (b,e), (c,e), (d,e)`，move 边为 `(c,d)`。若要合并 c,d，能够使用哪种策略？

A. George  
B. Briggs  
C. Both  
D. Neither

<details class="answer-drawer">
<summary>参考答案</summary>
<p>A。George 准则检查 c 的高阶邻居是否也与 d 相邻；Briggs 准则检查合并后高度数邻居数是否小于 K。按常见 K=3 课堂设定，本题可用 George 而 Briggs 不一定成立。</p>
</details>

### 选择题解析速查

| 题号 | 关键判断依据 |
|---:|---|
| 1 | 3 个余数状态说明 `m=3`；追加二进制位的余数更新是 `(2r+bit) mod 3`。 |
| 2 | `factor -> "(" exp ")" | NUM`，所以 FIRST 是 `{ "(", NUM }`；FOLLOW 沿 `term/factor` 上层传播，含 `* + - ) $`。 |
| 3 | LR 栈题要按表 shift/reduce；完整项规约后弹 RHS，再按 goto 入新状态。若回忆表给状态 5，则选 `S5`。 |
| 4 | Yacc 右部语义值从 1 编号：`exp PLUS pos exp` 中 `pos` 是 `$3`。 |
| 5 | LR 动作是 shift/reduce/accept/error；`match` 是 LL/递归下降里匹配终结符的说法。 |
| 6 | frame-resident local 通常用 `fp + offset`；`sp` 会随调用/压栈变化。 |
| 7 | 内层函数访问外层局部变量靠 static link 链或 display；单选选 static link。 |
| 8 | frame 可保存 old fp、return address、参数、局部、saved registers；全局/静态变量本体不在普通调用帧。 |
| 9 | functional symbol table 需要 persistent old versions，平衡树比 destructive hash table 更自然。 |
| 10 | tiling 是 instruction selection；physical register allocation 是后续阶段。 |
| 11 | DP tiling 是 bottom-up cost computation；`minimal munch` 不是标准算法。 |
| 12 | tree grammar 描述 IR tree patterns，可用于 code-generator generator 和 optimal tiling。 |
| 13 | liveness 方程直接用 `use/def/succ`；`pred` 可辅助遍历但不是方程必需项。 |
| 14 | interference graph 表示不能共寄存器的 live range 冲突。 |
| 15 | canonical tree 不含 `ESEQ/SEQ`，`CALL` 不深嵌；纯 `BINOP` 表达式合法。 |
| 16 | 每个 `CJUMP` 至少结束一个 block，串起来最少形成 `k+1` 个 block。 |
| 17 | 规范化把 `ESEQ` 的 statement 外提；必要时用 temp 保存不可交换表达式。 |
| 18 | 循环不变：操作数是常量，或所有 reaching definitions 都在循环外；唯一 reaching def 若在循环内仍不 invariant。 |
| 19 | non-moving mark-sweep 会产生 external fragmentation；copying GC 会 compact。 |
| 20 | George/Briggs 都是保守合并准则；按题设常见 `K=3`，该图满足 George 而 Briggs 不一定满足。 |

## 三、大题

### 1. 有符号数（带浮点带指数）的 DFA

构造一个 DFA，识别如下 token：可选正负号、整数部分、可选小数部分、可选指数部分。合法例子包括 `0`、`-12`、`+3.14`、`.5`、`1.`、`6.02e23`、`-1.0E-3`；非法例子包括 `+`、`.`、`1e`、`1e+`。

<details class="answer-drawer">
<summary>参考答案</summary>

把 token 分成三段：

```text
sign? number exponent?
number   = digits | digits "." digits* | "." digits
exponent = (e|E) sign? digits
```

状态表：

| 状态 | 含义 | 是否接受 |
|---|---|---|
| `S` | 起点 | 否 |
| `SIGN` | 已读 `+/-` | 否 |
| `INT` | 已读至少一个整数位 | 是 |
| `DOT_NO_INT` | 只读到 `.`，前面没有数字 | 否 |
| `FRAC` | 已进入小数部分，`1.` 和 `.5` 都到这里 | 是 |
| `EXP` | 刚读到 `e/E` | 否 |
| `EXP_SIGN` | 指数部分刚读到 `+/-` | 否 |
| `EXP_INT` | 指数部分已有至少一位数字 | 是 |
| `DEAD` | 非法 | 否 |

转移：

| 当前 | sign | digit | `.` | `e/E` | other |
|---|---|---|---|---|---|
| `S` | `SIGN` | `INT` | `DOT_NO_INT` | `DEAD` | `DEAD` |
| `SIGN` | `DEAD` | `INT` | `DOT_NO_INT` | `DEAD` | `DEAD` |
| `INT` | `DEAD` | `INT` | `FRAC` | `EXP` | `DEAD` |
| `DOT_NO_INT` | `DEAD` | `FRAC` | `DEAD` | `DEAD` | `DEAD` |
| `FRAC` | `DEAD` | `FRAC` | `DEAD` | `EXP` | `DEAD` |
| `EXP` | `EXP_SIGN` | `EXP_INT` | `DEAD` | `DEAD` | `DEAD` |
| `EXP_SIGN` | `DEAD` | `EXP_INT` | `DEAD` | `DEAD` | `DEAD` |
| `EXP_INT` | `DEAD` | `EXP_INT` | `DEAD` | `DEAD` | `DEAD` |
| `DEAD` | `DEAD` | `DEAD` | `DEAD` | `DEAD` | `DEAD` |

样例检查：

```text
0          S -> INT                         accept
-12        S -> SIGN -> INT                 accept
+3.14      S -> SIGN -> INT -> FRAC         accept
.5         S -> DOT_NO_INT -> FRAC          accept
1.         S -> INT -> FRAC                 accept
6.02e23    ... -> FRAC -> EXP -> EXP_INT    accept
-1.0E-3    ... -> FRAC -> EXP -> EXP_SIGN -> EXP_INT accept
+          SIGN                             reject
.          DOT_NO_INT                       reject
1e         EXP                              reject
1e+        EXP_SIGN                         reject
```

核心检查点：指数部分如果出现 `e/E`，后面必须至少有一位数字。

</details>

### 2. LALR(1) DFA 和 parsing table

对文法 `S' -> S; S -> L = R | R; L -> * R | id; R -> L` 构造 LR(1) 项集族，合并同 LR(0) core 的项得到 LALR(1) DFA，并给出 ACTION/GOTO 表，标出是否存在冲突。

<details class="answer-drawer">
<summary>参考答案</summary>

增广文法：

```text
0. S' → S
1. S  → L = R
2. S  → R
3. L  → * R
4. L  → id
5. R  → L
```

LR(1) closure 规则：

```text
若 [A → α . B β, a] 在项集中，
对 B → γ 加 [B → . γ, b]，
其中 b ∈ FIRST(βa)。
```

初始项集：

```text
I0:
  [S' → . S, $]
  [S  → . L = R, $]
  [S  → . R, $]
  [L  → . * R, =]
  [L  → . id, =]
  [R  → . L, $]
  [L  → . * R, $]
  [L  → . id, $]
```

关键 lookahead 来源：

```text
[S → . L = R, $]
  点后 L，β 是 "= R"
  FIRST(= R $) = {=}
  所以 L 产生式 lookahead 为 =

[S → . R, $]
  点后 R，β 为空
  FIRST($) = {$}
  所以 R 产生式 lookahead 为 $

[R → . L, $]
  点后 L，β 为空
  FIRST($) = {$}
  所以 L 产生式 lookahead 还包含 $
```

主要 goto：

```text
goto(I0, S):
  [S' → S ., $]        // accept on $

goto(I0, L):
  [S → L . = R, $]
  [R → L ., $]

goto(I0, R):
  [S → R ., $]

goto(I0, *):
  [L → * . R, =/$]
  [R → . L, =/$]
  [L → . * R, =/$]
  [L → . id, =/$]

goto(I0, id):
  [L → id ., =/$]
```

LALR 合并：

```text
只合并 LR(0) core 相同的状态。
合并时 lookahead 取并集。
```

这个经典文法在 canonical LR(1) 中无冲突，合并同 core 后仍无冲突，是 LALR(1)。

ACTION/GOTO 填表原则：

```text
含 [A → α . a β, x] 且 a 是终结符:
  ACTION[state,a] = shift goto(state,a)

含 [A → α ., x]:
  ACTION[state,x] = reduce A → α

含 [S' → S ., $]:
  ACTION[state,$] = accept
```

特别注意：

```text
状态 [S → L . = R, $] / [R → L ., $]
在 "=" 上 shift，在 "$" 上 reduce R → L。
```

这就是 LR(1) lookahead 避免冲突的关键。

</details>

### 3. static link 和 display

给定 Tiger 伪代码：`let function f(a:int)= let var x:=a function g(b:int)= x+b function h(c:int)= g(c)+x in h(3) end in f(2) end`。画出调用 `f -> h -> g` 时的活动记录关系，分别说明 static link 和 display 如何访问 `x`。

<details class="answer-drawer">
<summary>参考答案</summary>

词法结构：

```text
outer let
  f
    local x
    g
    h
```

`g` 和 `h` 都直接定义在 `f` 内，不是 `h` 定义在 `g` 内。

调用 `f -> h -> g` 时：

```text
f frame:
  a
  x

h frame:
  static link -> f frame
  c

g frame:
  static link -> f frame
  b
```

为什么 `h` 调 `g` 时，`g.static_link` 也指向 `f frame`：

```text
g 的词法外层是 f
不是调用者 h
```

访问 `x`：

```text
h 中访问 x:
  f_fp = MEM(h_fp + staticLinkOffset)
  x    = MEM(f_fp + offset_x)

g 中访问 x:
  f_fp = MEM(g_fp + staticLinkOffset)
  x    = MEM(f_fp + offset_x)
```

display 做法：

```text
display[level(f)] = f frame pointer
display[level(g)] = g frame pointer when in g
display[level(h)] = h frame pointer when in h
```

`g/h` 访问 `x` 时都直接：

```text
f_fp = display[level(f)]
x = MEM(f_fp + offset_x)
```

答案重点：static link 由“函数定义在哪一层”决定，不由“谁调用它”决定。

</details>

### 4. Tiger 语法的 IR 翻译

将 Tiger 表达式 `while i < n do (a[i] := a[i] + 1; i := i + 1)` 翻译成 Tree IR，要求体现条件跳转、循环 label、数组下标地址计算和赋值。

<details class="answer-drawer">
<summary>参考答案</summary>

先写循环控制流模板：

```text
LABEL Ltest
CJUMP(LT, TEMP i, TEMP n, Lbody, Ldone)

LABEL Lbody
  body statements
JUMP(NAME Ltest)

LABEL Ldone
```

数组元素地址：

```text
addr_i =
  BINOP(PLUS,
    TEMP a,
    BINOP(MUL, TEMP i, CONST wordSize))
```

若 Tiger 数组对象有 header，`TEMP a` 应先调整到首元素地址；本题按简化数组基址处理。

`a[i] := a[i] + 1`：

```text
MOVE(
  MEM(addr_i),
  BINOP(PLUS, MEM(addr_i), CONST 1))
```

`i := i + 1`：

```text
MOVE(TEMP i, BINOP(PLUS, TEMP i, CONST 1))
```

完整框架：

```text
LABEL Ltest
CJUMP(LT, TEMP i, TEMP n, Lbody, Ldone)

LABEL Lbody
MOVE(
  MEM(BINOP(PLUS, TEMP a,
      BINOP(MUL, TEMP i, CONST wordSize))),
  BINOP(PLUS,
    MEM(BINOP(PLUS, TEMP a,
        BINOP(MUL, TEMP i, CONST wordSize))),
    CONST 1))

MOVE(TEMP i, BINOP(PLUS, TEMP i, CONST 1))
JUMP(NAME Ltest)

LABEL Ldone
```

如果担心 `addr_i` 被重复计算，可先放入临时变量：

```text
MOVE(TEMP addr, ...)
MOVE(MEM(TEMP addr), BINOP(PLUS, MEM(TEMP addr), CONST 1))
```

</details>

### 5. Basic block 和最长 trace

给定线性 IR：`LABEL L1; CJUMP(<, a, b, L2, L3); LABEL L2; MOVE(x,1); JUMP L4; LABEL L3; MOVE(x,2); LABEL L4; MOVE(y,x); CJUMP(>, y,0,L5,L6); LABEL L5; MOVE(z,y); JUMP L7; LABEL L6; MOVE(z,0); LABEL L7; RETURN z`。划分 basic blocks 并找一个最长 trace。

<details class="answer-drawer">
<summary>参考答案</summary>

basic block 规则：

```text
1. 以 LABEL 开始。
2. 以 JUMP/CJUMP/RETURN 结束。
3. 中间不能有 LABEL 或跳转。
4. fall-through 到下一个 LABEL 时，必要时补 JUMP。
```

划分：

```text
B1:
  LABEL L1
  CJUMP(<, a, b, L2, L3)

B2:
  LABEL L2
  MOVE(x, 1)
  JUMP L4

B3:
  LABEL L3
  MOVE(x, 2)
  JUMP L4

B4:
  LABEL L4
  MOVE(y, x)
  CJUMP(>, y, 0, L5, L6)

B5:
  LABEL L5
  MOVE(z, y)
  JUMP L7

B6:
  LABEL L6
  MOVE(z, 0)
  JUMP L7

B7:
  LABEL L7
  RETURN z
```

CFG：

```text
B1 -> B2 / B3
B2 -> B4
B3 -> B4
B4 -> B5 / B6
B5 -> B7
B6 -> B7
```

一种 longest trace：

```text
B1 -> B2 -> B4 -> B5 -> B7
```

剩余：

```text
B3
B6
```

若想让 false fall-through，可能需要交换 `CJUMP` 条件或补 `JUMP`。trace schedule 的答案不唯一，只要每个 block 覆盖一次且控制流保持正确即可。

</details>

### 6. maximal munch 指令选择

对 IR 树 `MOVE(TEMP t1, MEM(BINOP(PLUS, TEMP fp, CONST -8)))` 与 `MOVE(MEM(BINOP(PLUS, TEMP fp, CONST -12)), BINOP(PLUS, TEMP t1, CONST 1))`，用 maximal munch 在含 `load r <- M[base+k]`、`store M[base+k] <- r`、`addi r <- r+k` 的机器上生成指令。

<details class="answer-drawer">
<summary>参考答案</summary>

第一棵树：

```text
MOVE(TEMP t1,
     MEM(BINOP(PLUS, TEMP fp, CONST -8)))
```

最大 tile 匹配：

```text
MEM(BINOP(PLUS, base, CONST k))
→ load r <- M[base+k]
```

生成：

```text
load t1 <- M[fp-8]
```

第二棵树：

```text
MOVE(
  MEM(BINOP(PLUS, TEMP fp, CONST -12)),
  BINOP(PLUS, TEMP t1, CONST 1))
```

右侧先匹配 immediate add：

```text
addi r <- t1, 1
```

左侧匹配 store addressing mode：

```text
store M[fp-12] <- r
```

最终：

```text
load t1 <- M[fp-8]
addi r <- t1, 1
store M[fp-12] <- r
```

如果目标机器支持：

```text
store M[base+k] <- reg+imm
```

才可能用更大 tile；题目给定模板下需要临时寄存器 `r`。

</details>

### 7. 活跃分析

对指令序列：`1: a <- 1; 2: b <- 2; 3: c <- a + b; 4: b <- c + a; 5: if b < 10 goto 3; 6: return b`，写出 use/def/succ，并迭代求每条指令的 in/out。

<details class="answer-drawer">
<summary>参考答案</summary>

use/def/succ：

| 行 | 语句 | use | def | succ |
|---|---|---|---|---|
| 1 | `a <- 1` | `{}` | `{a}` | `{2}` |
| 2 | `b <- 2` | `{}` | `{b}` | `{3}` |
| 3 | `c <- a + b` | `{a,b}` | `{c}` | `{4}` |
| 4 | `b <- c + a` | `{c,a}` | `{b}` | `{5}` |
| 5 | `if b < 10 goto 3` | `{b}` | `{}` | `{3,6}` |
| 6 | `return b` | `{b}` | `{}` | `{}` |

方程：

```text
out[n] = union(in[s] for s in succ[n])
in[n]  = use[n] union (out[n] - def[n])
```

固定点结果：

| 行 | in | out |
|---|---|---|
| 1 | `{}` | `{a}` |
| 2 | `{a}` | `{a,b}` |
| 3 | `{a,b}` | `{a,c}` |
| 4 | `{a,c}` | `{a,b}` |
| 5 | `{a,b}` | `{a,b}` |
| 6 | `{b}` | `{}` |

关键代入：

```text
out[5] = in[3] ∪ in[6]
       = {a,b} ∪ {b}
       = {a,b}

in[5] = use[5] ∪ (out[5] - def[5])
      = {b} ∪ ({a,b} - {})
      = {a,b}
```

循环回边让 `a` 和 `b` 在 5 的 out 中保持活跃。

</details>

### 8. 带 priority 的寄存器分配

给定临时变量权重：`a: loop use 8, b: loop use 5, c: loop use 3, d: loop use 1`，相干边：`(a,b),(a,c),(b,c),(b,d),(c,d)`，机器寄存器 K=2。计算 spill priority，并说明 simplify/select/spill 的选择。

<details class="answer-drawer">
<summary>参考答案</summary>

边：

```text
(a,b),(a,c),(b,c),(b,d),(c,d)
K = 2
```

degree：

```text
deg(a)=2  // b,c
deg(b)=3  // a,c,d
deg(c)=3  // a,b,d
deg(d)=2  // b,c
```

priority：

```text
priority = cost / degree
```

| temp | cost | degree | priority |
|---|---:|---:|---:|
| `a` | 8 | 2 | 4.00 |
| `b` | 5 | 3 | 1.67 |
| `c` | 3 | 3 | 1.00 |
| `d` | 1 | 2 | 0.50 |

低 priority 更适合 spill，所以候选顺序：

```text
d, c, b, a
```

但 simplify 条件是：

```text
degree < K
```

本图初始所有节点 degree 都 ≥ 2，没有低度节点。因此可先选最低 priority 的 `d` 作为 potential spill：

```text
push d as potential spill
remove d
```

删除 `d` 后：

```text
deg(b)=2
deg(c)=2
deg(a)=2
```

若仍无低度节点，继续按策略选 potential spill；也可在实际算法中结合 optimistic coloring，先压栈，select 阶段再看是否真无颜色。

actual spill 判断在 select 阶段：

```text
若弹出 d 时两个颜色都被已着色邻居占用，则 d actual spill。
否则 d 可着色，不需要 rewrite。
```

如果 actual spill：

```text
每次 use d 前插入 load
每次 def d 后插入 store
重新做 liveness 和 register allocation
```

</details>
