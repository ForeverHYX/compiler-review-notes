# 编译原理模拟期末卷 B
Date: 2026-06-23
Author: Yixun Hong
Tags: Compiler, Mock Final, IR, Trace, GC
Abstract: 第二套模拟卷，偏中后端：规范化、basic block、trace scheduling、指令选择、循环优化和垃圾回收。

## 一、选择题

1. `ESEQ(s,e)` 的主要含义是：

A. 先执行语句 s，再计算表达式 e  
B. 同时执行 s 和 e  
C. 删除 s，只保留 e  
D. 表示一个基本块

<details class="answer-drawer">
<summary>参考答案</summary>

A。

`ESEQ(s, e)` 的语义是：

```text
先执行 statement s
再计算 expression e
整个 ESEQ 的值就是 e 的值
```

例子：

```text
ESEQ(MOVE(TEMP a, CONST 1), TEMP a)
```

表示：

```text
a <- 1
结果值为 a
```

规范化阶段会尽量把 `ESEQ` 里的 statement 提到外面，避免副作用藏在表达式内部。

</details>

2. basic block 的最后一条语句通常是：

A. LABEL  
B. JUMP/CJUMP/RETURN  
C. MOVE  
D. BINOP

<details class="answer-drawer">
<summary>参考答案</summary>

B。

basic block 的标准形式：

```text
LABEL ...
普通语句 ...
JUMP / CJUMP / RETURN
```

要求：

```text
1. 只能从第一条进入。
2. 只能从最后一条离开。
3. 中间不能有 LABEL。
4. 中间不能有 JUMP/CJUMP。
```

因此最后一条通常是 `JUMP`、`CJUMP` 或 `RETURN`。

</details>

3. copying collection 相比 mark-sweep 的优势之一是：

A. 不需要 root set  
B. 可以压缩存活对象，减少外部碎片  
C. 无需移动对象  
D. 不能处理循环引用

<details class="answer-drawer">
<summary>参考答案</summary>

B。

copying collection 的过程：

```text
from-space 中找 live objects
复制到 to-space
复制时连续排列
roots 和对象内部指针改到新地址
```

结果：

```text
live objects 被压缩到一起
free pointer 后面是一整段连续空闲空间
```

所以它可以减少 external fragmentation，并且分配通常可以用 bump pointer。

对比：

```text
mark-sweep 不移动对象，可能留下很多小空洞。
copying GC 会移动对象，所以需要更新指针。
copying GC 仍需要 root set。
copying GC 可以处理循环引用，因为基于 reachability。
```

</details>

## 二、大题

### 1. 正则树规范化

规范化 `BINOP(PLUS, ESEQ(MOVE(TEMP a, CONST 1), TEMP a), CALL(NAME f, []))`，要求 CALL 不作为普通表达式深嵌。

<details class="answer-drawer">
<summary>参考答案</summary>

原表达式：

```text
BINOP(PLUS,
  ESEQ(MOVE(TEMP a, CONST 1), TEMP a),
  CALL(NAME f, []))
```

第一步：提升左侧 `ESEQ`。

```text
先执行:
  MOVE(TEMP a, CONST 1)

剩余表达式:
  BINOP(PLUS, TEMP a, CALL(NAME f, []))
```

第二步：`CALL` 不能作为普通表达式深嵌在 `BINOP` 里。引入临时变量：

```text
MOVE(TEMP tcall, CALL(NAME f, []))
```

然后表达式改为：

```text
BINOP(PLUS, TEMP a, TEMP tcall)
```

最终 canonical 形式可写成：

```text
statement list:
  MOVE(TEMP a, CONST 1)
  MOVE(TEMP tcall, CALL(NAME f, []))

expression:
  BINOP(PLUS, TEMP a, TEMP tcall)
```

检查点：

```text
1. 没有 ESEQ。
2. CALL 只出现在 MOVE(TEMP, CALL(...)) 这种顶层安全位置。
3. 求值顺序仍是先 a<-1，再调用 f，再做加法。
```

</details>

### 2. Trace scheduling

给定 CFG 边：B1->B2/B3，B2->B4，B3->B4，B4->B5/B6，B5->B7，B6->B7。选择 trace 并说明如何处理条件跳转 fall-through。

<details class="answer-drawer">
<summary>参考答案</summary>

CFG：

```text
B1 -> B2 / B3
B2 -> B4
B3 -> B4
B4 -> B5 / B6
B5 -> B7
B6 -> B7
```

一种 trace 选择：

```text
Trace 1: B1 -> B2 -> B4 -> B5 -> B7
Trace 2: B3
Trace 3: B6
```

选择理由：

```text
B1 选一个后继 B2
B2 唯一后继 B4
B4 选一个后继 B5
B5 唯一后继 B7
```

处理 `CJUMP` fall-through：

```text
如果 B1 的 CJUMP 选择 B2 作为 trace 后继，
最好让 B2 紧跟 B1，作为 fall-through 分支。

若原 CJUMP 是:
  CJUMP(cond, true=B3, false=B2)
则 B2 已经是 false fall-through，可以保留。

若原 CJUMP 是:
  CJUMP(cond, true=B2, false=B3)
但机器/规范希望 false fall-through，
可改成:
  CJUMP(not cond, true=B3, false=B2)
```

如果无法通过交换条件满足布局，就补偿：

```text
CJUMP(cond, Ltrue, Lfalse)
LABEL fallThroughBlock
...
JUMP realTarget
```

目标是减少无条件跳转，同时保持控制流语义不变。

</details>

### 3. Maximal munch 与 dynamic programming

同一棵树若 maximal munch 得到代价 4，动态规划得到代价 3，说明原因，并给出何时仍会使用 maximal munch。

<details class="answer-drawer">
<summary>参考答案</summary>

原因：

```text
maximal munch:
  从 root 开始贪心选“当前能覆盖的最大 tile”。

dynamic programming:
  自底向上计算每个节点所有可选 tile 的最低 cost。
```

maximal munch 得到代价 4，而 DP 得到代价 3，说明：

```text
root 处最大的 tile 不一定是全局最低成本组合的一部分。
贪心选择可能让子树只能用较贵 tile 覆盖。
```

DP 的典型表：

| 节点 | 可选 tile | 子树代价 | 本 tile 代价 | 总代价 |
|---|---|---:|---:|---:|
| `n` | tile A | ... | ... | 4 |
| `n` | tile B | ... | ... | 3 |

DP 会选总代价 3 的 tile B。

仍使用 maximal munch 的场景：

```text
1. 编译速度比目标代码质量更重要。
2. 指令集简单，贪心结果足够好。
3. 教学/小型编译器实现简单。
4. 后续优化或寄存器分配会进一步改善代码。
```

术语提醒：

```text
maximal munch may be optimal in textbook local sense,
but not necessarily optimum / lowest-cost.
```

</details>

### 4. 循环不变式外提

循环中有 `t <- a * 4; x <- M[base+t]; i <- i+1`，其中 a 在循环内无定义。哪些语句可外提？需要满足什么安全条件？

<details class="answer-drawer">
<summary>参考答案</summary>

循环语句：

```text
t <- a * 4
x <- M[base + t]
i <- i + 1
```

已知：

```text
a 在循环内无定义
```

判断 `t <- a * 4`：

```text
a 是循环不变量
4 是常量
a * 4 无副作用
```

所以 `t <- a * 4` 是 loop-invariant computation，可以考虑外提到 preheader：

```text
preheader:
  t <- a * 4

loop:
  x <- M[base+t]
  i <- i+1
```

但能否安全外提还要满足：

```text
1. loop 内没有其他 t 定义。
2. t 不在 preheader live-out，避免覆盖进入循环前还要用的旧 t。
3. 该定义支配循环内所有使用 t 的点。
4. 若 t 在 loop exit 后 live，该定义要支配相关 exits。
```

判断 `x <- M[base+t]`：

```text
t invariant 不代表 M[base+t] invariant。
```

还需要证明：

```text
base 循环内不变
循环内没有 store 可能写到 M[base+t]
load 不会因提前执行改变异常行为
该内存位置不会被调用或别名修改
```

如果这些都能证明，load 也可能外提；否则只能外提地址计算 `t <- a*4`，保留 load 在循环内。

</details>
