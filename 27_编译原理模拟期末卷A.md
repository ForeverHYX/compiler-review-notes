# 编译原理模拟期末卷 A
Date: 2026-06-23
Author: Yixun Hong
Tags: Compiler, Mock Final, DFA, LALR, Register Allocation
Abstract: 第一套模拟卷，覆盖词法 DFA、LL/LR、活动记录、IR 翻译、trace、指令选择、活跃分析和寄存器分配。

## 一、判断题

1. 一个正则语言一定可以由 DFA 识别。

<details class="answer-drawer">
<summary>参考答案</summary>

对。

经典等价关系：

```text
regular expression ⇔ NFA ⇔ DFA
```

证明/构造链条：

```text
RE 可以用 Thompson construction 构造 NFA。
NFA 可以用 subset construction 构造 DFA。
DFA 显然也是一种 NFA。
```

所以任何正则语言都一定存在一个 DFA 识别它。

</details>

2. 所有 LR(1) 文法都是 LALR(1) 文法。

<details class="answer-drawer">
<summary>参考答案</summary>

错。

LALR(1) 的做法是：

```text
先构造 canonical LR(1) 项集
再把 LR(0) core 相同的状态合并
lookahead 集合取并集
```

合并可能把两个原本分开的 reduce lookahead 合在一起，从而产生新的 reduce/reduce conflict。

所以：

```text
LALR(1) ⊂ LR(1)
```

不是所有 LR(1) 文法都是 LALR(1) 文法。

</details>

3. 在 Tiger 中 escaping 的局部变量可以安全放入 caller-save 寄存器。

<details class="answer-drawer">
<summary>参考答案</summary>

错。

escaping 的局部变量可能被内层函数访问，或地址被传出。它必须有稳定的内存位置：

```text
frame slot = fp + offset
```

caller-save 寄存器在函数调用后可能被 callee 覆盖，且没有稳定地址，内层函数无法通过 static link 找到它。

所以 escaping local 通常放在 stack frame 中，而不是 caller-save register 中。

</details>

4. 活跃性分析是后向 may analysis。

<details class="answer-drawer">
<summary>参考答案</summary>

对。

活跃变量定义：

```text
变量 v 在程序点 p 活跃
⇔ 存在一条从 p 出发的路径，未来会使用 v，且中间没有重新定义 v
```

因此它是 may analysis：只要有一条未来路径可能使用，就认为 live。

方程也体现了后向：

```text
out[n] = union(in[s] for s in succ[n])
in[n]  = use[n] union (out[n] - def[n])
```

`in[n]` 依赖后继信息，所以是 backward analysis。

</details>

## 二、选择题

1. 文法 `E -> E + T | T; T -> id` 直接用于 LL(1) 分析的问题是：

A. 存在左递归  
B. 没有终结符  
C. 没有开始符号  
D. 不是上下文无关文法

<details class="answer-drawer">
<summary>参考答案</summary>

A。

文法：

```text
E -> E + T | T
```

有直接左递归：

```text
E ⇒ E + T
```

递归下降函数如果按产生式直接写：

```text
parseE():
  parseE()
  match(+)
  parseT()
```

会在没有读入任何 token 的情况下再次调用 `parseE()`，导致无限递归。

LL(1) 分析要求能用一个 lookahead 决定产生式；直接左递归文法不适合 LL(1)，应先改写成：

```text
E  -> T E'
E' -> + T E' | ε
```

</details>

2. 在 LR parsing table 中，某状态同一终结符下既有 shift 又有 reduce，称为：

A. reduce/reduce conflict  
B. shift/reduce conflict  
C. FIRST/FOLLOW conflict  
D. type conflict

<details class="answer-drawer">
<summary>参考答案</summary>

B。

LR 表中某个格子：

```text
ACTION[state, terminal]
```

如果同时需要：

```text
shift  terminal
reduce A -> α
```

就是 shift/reduce conflict。

reduce/reduce conflict 则是同一格里有两条不同 reduce。

</details>

3. 以下哪个最适合表示 `a[i]` 的地址？

A. `BINOP(PLUS, TEMP a, BINOP(MUL, TEMP i, CONST wordSize))`  
B. `CALL(NAME a, TEMP i)`  
C. `SEQ(TEMP a, TEMP i)`  
D. `CJUMP(EQ, a, i, L1, L2)`

<details class="answer-drawer">
<summary>参考答案</summary>

A。

数组元素地址公式：

```text
address(a[i]) = base(a) + i * wordSize
```

对应 Tree IR：

```text
BINOP(PLUS,
  TEMP a,
  BINOP(MUL, TEMP i, CONST wordSize))
```

若要取元素值，还要再包一层：

```text
MEM(address)
```

本题问“地址”，所以选不带外层 `MEM` 的 A。

</details>

## 三、大题

### 1. DFA 构造

构造 DFA 识别十六进制整数 token：可选前缀 `0x` 或 `0X`，之后至少一个十六进制数字；无前缀时只允许十进制数字。说明接受状态。

<details class="answer-drawer">
<summary>参考答案</summary>

先定义字符类：

```text
digit    = 0-9
nonzero  = 1-9
hexDigit = 0-9 a-f A-F
```

状态：

| 状态 | 含义 | 是否接受 |
|---|---|---|
| `Start` | 尚未读入字符 | 否 |
| `Zero` | 只读到开头 `0` | 是 |
| `Dec` | 正在读十进制整数 | 是 |
| `X` | 已读到 `0x` 或 `0X`，等待至少一个 hex digit | 否 |
| `Hex` | 正在读十六进制数字 | 是 |
| `Dead` | 非法 | 否 |

转移：

| 当前 | `0` | `1-9` | `x/X` | `a-f/A-F` | other |
|---|---|---|---|---|---|
| `Start` | `Zero` | `Dec` | `Dead` | `Dead` | `Dead` |
| `Zero` | `Dec` | `Dec` | `X` | `Dead` | `Dead` |
| `Dec` | `Dec` | `Dec` | `Dead` | `Dead` | `Dead` |
| `X` | `Hex` | `Hex` | `Dead` | `Hex` | `Dead` |
| `Hex` | `Hex` | `Hex` | `Dead` | `Hex` | `Dead` |
| `Dead` | `Dead` | `Dead` | `Dead` | `Dead` | `Dead` |

检查：

```text
0       Start -> Zero              accept
123     Start -> Dec -> Dec        accept
0xF     Start -> Zero -> X -> Hex  accept
0X12a   ... -> Hex                 accept
0x      ends at X                  reject
x12     Start -> Dead              reject
12a     Dec -> Dead on a           reject
```

</details>

### 2. LALR(1) 分析

对 `S'->S; S->AA; A->aA|b` 构造 LR(1) 初始项集 I0，并写出 `goto(I0, A)` 与 `goto(I0, a)` 的核心。

<details class="answer-drawer">
<summary>参考答案</summary>

文法：

```text
S' -> S
S  -> A A
A  -> a A | b
```

LR(1) closure 规则：

```text
[X -> α . Y β, z]
加入 [Y -> . γ, t]
其中 t ∈ FIRST(βz)
```

`I0`：

```text
[S' -> . S, $]
[S  -> . A A, $]
```

对 `[S -> . A A, $]`：

```text
βz = A$
FIRST(A$) = {a,b}
```

所以加入：

```text
[A -> . a A, a]
[A -> . a A, b]
[A -> . b, a]
[A -> . b, b]
```

紧凑写法：

```text
I0:
  [S' -> . S, $]
  [S  -> . A A, $]
  [A  -> . a A, a/b]
  [A  -> . b, a/b]
```

`goto(I0, A)`：

```text
先移动点:
  [S -> A . A, $]

点后仍是 A，β 为空，lookahead 是 $:
  FIRST($) = {$}

closure 加:
  [A -> . a A, $]
  [A -> . b, $]
```

所以 core 是：

```text
S -> A . A
```

`goto(I0, a)`：

```text
移动点:
  [A -> a . A, a]
  [A -> a . A, b]

closure:
  对 lookahead a 加 [A -> .aA,a], [A -> .b,a]
  对 lookahead b 加 [A -> .aA,b], [A -> .b,b]
```

紧凑写：

```text
[A -> a . A, a/b]
[A -> . a A, a/b]
[A -> . b, a/b]
```

</details>

### 3. Tiger IR

将 `for i := 0 to n do s := s + i` 翻译成 IR 控制流框架。

<details class="answer-drawer">
<summary>参考答案</summary>

Tiger `for i := lo to hi do body` 的关键点：

```text
1. 初始化 i。
2. 上界 hi 只求值一次，保存到 limit。
3. 每轮先判断 i <= limit。
4. body 后如果 i < limit，才 i := i + 1 并回到 test。
5. 避免 i == maxint 时 i+1 溢出。
```

代入本题：

```text
lo = 0
hi = n
body = s := s + i
```

控制流框架：

```text
MOVE(TEMP i, CONST 0)
MOVE(TEMP limit, TEMP n)

LABEL Ltest
CJUMP(LE, TEMP i, TEMP limit, Lbody, Ldone)

LABEL Lbody
MOVE(TEMP s, BINOP(PLUS, TEMP s, TEMP i))

CJUMP(LT, TEMP i, TEMP limit, Linc, Ldone)

LABEL Linc
MOVE(TEMP i, BINOP(PLUS, TEMP i, CONST 1))
JUMP(NAME Ltest)

LABEL Ldone
```

为什么 body 后还要 `i < limit`：

```text
如果 i 已经等于 limit，body 已执行最后一次；
此时不应再做 i+1，否则可能溢出。
```

</details>

### 4. 活跃分析与相干图

对 `a<-1; b<-2; c<-a+b; d<-c+b; return d` 求 live-out，并画相干边。

<details class="answer-drawer">
<summary>参考答案</summary>

编号：

```text
1: a <- 1
2: b <- 2
3: c <- a + b
4: d <- c + b
5: return d
```

use/def：

| 行 | use | def |
|---|---|---|
| 1 | `{}` | `{a}` |
| 2 | `{}` | `{b}` |
| 3 | `{a,b}` | `{c}` |
| 4 | `{c,b}` | `{d}` |
| 5 | `{d}` | `{}` |

线性控制流：

```text
succ[1]={2}, succ[2]={3}, succ[3]={4}, succ[4]={5}, succ[5]={}
```

方程：

```text
out[n] = union(in[s] for s in succ[n])
in[n]  = use[n] union (out[n] - def[n])
```

从后往前：

| 行 | in | out |
|---|---|---|
| 5 | `{d}` | `{}` |
| 4 | `{b,c}` | `{d}` |
| 3 | `{a,b}` | `{b,c}` |
| 2 | `{a}` | `{a,b}` |
| 1 | `{}` | `{a}` |

干涉边按 def 与 live-out 建：

```text
line 1 def a, out {a}: 无自边
line 2 def b, out {a,b}: b-a
line 3 def c, out {b,c}: c-b
line 4 def d, out {d}: 无自边
```

所以主要边：

```text
(a,b), (b,c)
```

`d` 只在 return 前短暂 live，不与其他变量同时 live。

</details>
