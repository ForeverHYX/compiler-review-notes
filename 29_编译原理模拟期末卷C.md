# 编译原理模拟期末卷 C
Date: 2026-06-23
Author: Yixun Hong
Tags: Compiler, Mock Final, Semantic Analysis, Allocation
Abstract: 第三套模拟卷，综合语义分析、函数调用约定、闭包访问、数据流方程和图着色寄存器分配。

## 一、判断题

1. FIRST/FOLLOW 集只用于 LL 分析，和 LR 分析完全无关。

<details class="answer-drawer">
<summary>参考答案</summary>

错。

FIRST/FOLLOW 最常见于 LL(1)，但不只用于 LL。

LR(1) closure 中也要计算 FIRST：

```text
若 [A → α . B β, a] 在项集中，
对 B → γ 加入 [B → . γ, b]
其中 b ∈ FIRST(βa)
```

SLR 还会用 FOLLOW 限制 reduce：

```text
LR(0) 完整项 A → α .
SLR 只在 FOLLOW(A) 上填 reduce
```

所以 FIRST/FOLLOW 和 LR 分析也有关。

</details>

2. caller-save 寄存器在调用后由调用者负责保存需要的值。

<details class="answer-drawer">
<summary>参考答案</summary>

对。

caller-save 的含义：

```text
调用者如果还需要某个 caller-save register 中的值，
必须在 call 前保存，call 后恢复。
```

callee-save 的含义：

```text
被调用者如果使用 callee-save register，
必须在返回前恢复为进入函数时的值。
```

寄存器分配建模时，call 指令通常被看作：

```text
use: 参数寄存器
def: 返回值寄存器 + caller-save clobber registers
```

</details>

3. 若两个变量从不同时 live，则它们可以分配到同一个物理寄存器。

<details class="answer-drawer">
<summary>参考答案</summary>

对。

两个变量可以共用寄存器的核心条件：

```text
它们的 live ranges 不重叠
```

相干图中：

```text
有边  → 两个变量同时 live，不能同寄存器
无边  → 在 liveness 近似下可尝试同寄存器
```

所以“从不同时 live”意味着不需要同时保留两个值，可分配到同一个物理寄存器。

</details>

## 二、选择题

1. Tiger 中数组越界检查最适合放在哪个阶段附近？

A. 词法分析  
B. 语法分析  
C. IR 翻译/后续 lowering  
D. 链接阶段

<details class="answer-drawer">
<summary>参考答案</summary>

C。

数组越界检查需要运行时值：

```text
index >= 0
index < array.length
```

词法分析只看字符，语法分析只看结构，都不知道运行时下标值。

通常在 IR 翻译或后续 lowering 阶段把访问：

```text
a[i]
```

扩展成：

```text
if i < 0 or i >= length(a) then call bounds_error
else MEM(base(a) + i * wordSize)
```

后续循环优化可能再尝试删除或外提 bounds check。

</details>

2. Briggs 合并准则的直观含义是：

A. 合并后高度数邻居少于 K 个时较安全  
B. 只要两个节点有 move 边就必定合并  
C. 只检查一个节点的邻居是否都连接另一个节点  
D. 永远不合并 move-related 节点

<details class="answer-drawer">
<summary>参考答案</summary>

A。

Briggs conservative coalescing 的直觉：

```text
把两个 move-related 节点合并后，
若合并节点的 high-degree 邻居少于 K 个，
则认为合并是安全的。
```

其中 high-degree 通常指：

```text
degree >= K
```

C 更接近 George 准则：

```text
对于与 precolored 节点 r 合并的普通节点 n，
n 的每个邻居 t 要么已经与 r 相邻，
要么 t 是低度节点。
```

</details>

## 三、大题

### 1. 语义分析

分析 Tiger 表达式 `let type intArray = array of int var a := intArray[10] of 0 in a[0] := "x" end`，指出类型错误位置。

<details class="answer-drawer">
<summary>参考答案</summary>

表达式：

```tiger
let
  type intArray = array of int
  var a := intArray[10] of 0
in
  a[0] := "x"
end
```

环境构造：

```text
TEnv:
  intArray -> array of int

VEnv:
  a -> intArray
```

检查数组创建：

```text
intArray[10] of 0

size: 10 : int
init: 0 : int
element type of intArray = int
```

所以数组创建合法，表达式类型是 `intArray`。

检查赋值：

```text
a[0] 的类型:
  a : intArray
  intArray element type = int
  index 0 : int
  所以 a[0] : int

"x" : string
```

赋值要求左右类型一致：

```text
int := string
```

不匹配，因此错误位置在：

```text
a[0] := "x"
```

</details>

### 2. 函数调用与 static link

函数 `f` 定义在最外层，`g` 定义在 `f` 内，`h` 定义在 `g` 内。若 `f` 调用 `g`，`g` 调用 `h`，分别传递哪些 static link？

<details class="answer-drawer">
<summary>参考答案</summary>

词法嵌套：

```text
f
  g
    h
```

调用 `g` 时：

```text
g 定义在 f 内
所以 g.static_link = 当前 f frame
```

调用 `h` 时：

```text
h 定义在 g 内
所以 h.static_link = 当前 g frame
```

若 `h` 访问 `f` 的变量：

```text
h frame
  --static link--> g frame
  --static link--> f frame
  + offset_of_variable
```

易错点：

```text
static link 不是调用者链接。
它指向词法外层，不是动态调用者。
```

本题中刚好是 `f -> g -> h` 顺着词法层级调用，所以看起来和调用链一致；但概念上仍然要按“定义在哪一层”决定 static link。

</details>

### 3. 数据流分析

给定 CFG：B1->B2,B1->B3,B2->B4,B3->B4。各块 `use/def` 为：B1 use={},def={a}; B2 use={a},def={b}; B3 use={a},def={c}; B4 use={b,c},def={d}。求 live-in/live-out。

<details class="answer-drawer">
<summary>参考答案</summary>

CFG：

```text
B1 -> B2
B1 -> B3
B2 -> B4
B3 -> B4
```

use/def：

| 块 | use | def |
|---|---|---|
| `B1` | `{}` | `{a}` |
| `B2` | `{a}` | `{b}` |
| `B3` | `{a}` | `{c}` |
| `B4` | `{b,c}` | `{d}` |

方程：

```text
out[B] = union(in[S] for S in succ[B])
in[B]  = use[B] union (out[B] - def[B])
```

从出口往前：

```text
B4:
  out[B4] = {}
  in[B4] = {b,c} ∪ ({} - {d}) = {b,c}

B2:
  out[B2] = in[B4] = {b,c}
  in[B2] = {a} ∪ ({b,c} - {b}) = {a,c}

B3:
  out[B3] = in[B4] = {b,c}
  in[B3] = {a} ∪ ({b,c} - {c}) = {a,b}

B1:
  out[B1] = in[B2] ∪ in[B3]
          = {a,c} ∪ {a,b}
          = {a,b,c}
  in[B1] = {} ∪ ({a,b,c} - {a})
         = {b,c}
```

结果：

| 块 | in | out |
|---|---|---|
| `B1` | `{b,c}` | `{a,b,c}` |
| `B2` | `{a,c}` | `{b,c}` |
| `B3` | `{a,b}` | `{b,c}` |
| `B4` | `{b,c}` | `{}` |

这是保守 may analysis。`b` 和 `c` 分别在不同分支定义，但在 join 块 `B4` 同时被 use，因此分析会把它们都视为需要到达 `B4`。

</details>

### 4. 图着色寄存器分配

K=3，相干边为 `(a,b),(a,c),(a,d),(b,c),(c,d),(d,e)`，move 边为 `(b,d)`。说明 simplify、coalesce、freeze、spill 的一次可能过程。

<details class="answer-drawer">
<summary>参考答案</summary>

图：

```text
interference:
  (a,b),(a,c),(a,d),(b,c),(c,d),(d,e)
move:
  (b,d)
K = 3
```

初始 degree：

```text
deg(a)=3  // b,c,d
deg(b)=2  // a,c
deg(c)=3  // a,b,d
deg(d)=3  // a,c,e
deg(e)=1  // d
```

一个可能流程：

```text
1. Simplify e
   e 低度且非 move-related，push e。
   删除 e 后 deg(d) 从 3 降到 2。

2. 处理 move b<->d
   b 和 d 没有 interference edge，可尝试 coalesce。
```

Briggs 检查：

```text
neighbors(b ∪ d) = {a,c,e}
high-degree 邻居中 a,c 初始 degree 为 3，e 为 1
high-degree 数量 = 2 < K
```

所以按 Briggs 可认为合并安全。合并为 `bd` 后：

```text
bd 的邻居 = {a,c,e}
```

继续 simplify：

```text
删除 e 后 bd degree 可能变为 2
push bd
剩余 a,c 互相相邻，均可着色
```

若某个判据认为合并不安全，则流程可以是：

```text
Freeze move (b,d)
b 不再 move-related
再 simplify b
```

若剩余图没有 degree < K 的节点，才按 spill priority 选 potential spill。

select 阶段：

```text
从栈顶逆序弹出
给每个节点选择不与已着色邻居冲突的颜色
若 potential spill 也能着色，就不是 actual spill
```

本题主要考：simplify/coalesce/freeze/spill 的顺序，以及 move-related 节点不应无条件直接 spill。

</details>

### 5. IR 到汇编

为 `CALL(NAME print_int, [MEM(BINOP(PLUS, TEMP fp, CONST -8))])` 生成一段调用约定友好的伪汇编。

<details class="answer-drawer">
<summary>参考答案</summary>

IR：

```text
CALL(NAME print_int,
     [MEM(BINOP(PLUS, TEMP fp, CONST -8))])
```

参数表达式：

```text
MEM(fp - 8)
```

若目标机器第一参数寄存器是 `r_arg0`：

```text
load r_arg0 <- M[fp-8]
```

调用前要考虑 caller-save：

```text
若当前 live 的值在 caller-save 寄存器中，
并且 call 后还要用，
则 call 前 spill/save，call 后 restore。
```

伪汇编：

```text
// save live caller-save registers if needed
store M[spill_r1] <- r1        // 仅当 r1 call 后仍 live

load r_arg0 <- M[fp-8]
call print_int

// restore saved caller-save registers if needed
load r1 <- M[spill_r1]
```

如果返回值不用：

```text
忽略返回寄存器 r_ret
```

如果 `print_int` 是外部函数，还要遵守运行时调用约定，例如栈对齐、参数寄存器、返回地址和 caller-save clobber 集合。

</details>
