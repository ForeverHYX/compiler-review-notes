# 编译原理期末大题回忆卷
Date: 2024-06-21
Author: Yixun Hong
Tags: Compiler, Recall, Big Questions, Tiger
Abstract: 根据考后记忆整理的大题方向，保留原始回忆顺序，并补成可以直接练习的完整题干。

## 说明

这份是“原来那份回忆卷”的独立版本，只围绕记住的大题，不含 PDF 小题。题目按回忆补全，适合快速过一遍大题套路。

## 1. 有符号数（浮点数与指数）的 DFA

请构造 DFA 识别有符号数 token：`+12`、`-0.5`、`.25`、`3.`、`2e10`、`-6.02E+23` 合法；`+`、`.`、`e10`、`1e`、`1e+` 非法。写出状态集合、接受状态和转移。

<details class="answer-drawer">
<summary>参考答案</summary>

先把 token 结构拆开：

```text
sign? ((digits "." digits*) | ("." digits) | digits) exponent?
exponent = (e|E) sign? digits
```

状态含义：

| 状态 | 含义 | 是否接受 |
|---|---|---|
| `Start` | 尚未读入有效字符 | 否 |
| `Sign` | 只读到 `+` 或 `-` | 否 |
| `Int` | 已读到至少一个整数位 | 是 |
| `DotNoInt` | 只读到 `.`，前面没有整数 | 否 |
| `Frac` | 已读到小数形式，如 `3.`、`.5`、`3.14` | 是 |
| `ExpMark` | 刚读到 `e/E` | 否 |
| `ExpSign` | 指数部分刚读到 `+/-` | 否 |
| `ExpInt` | 指数部分已有至少一个数字 | 是 |
| `Dead` | 非法 | 否 |

转移表：

| 当前状态 | sign | digit | `.` | `e/E` | other |
|---|---|---|---|---|---|
| `Start` | `Sign` | `Int` | `DotNoInt` | `Dead` | `Dead` |
| `Sign` | `Dead` | `Int` | `DotNoInt` | `Dead` | `Dead` |
| `Int` | `Dead` | `Int` | `Frac` | `ExpMark` | `Dead` |
| `DotNoInt` | `Dead` | `Frac` | `Dead` | `Dead` | `Dead` |
| `Frac` | `Dead` | `Frac` | `Dead` | `ExpMark` | `Dead` |
| `ExpMark` | `ExpSign` | `ExpInt` | `Dead` | `Dead` | `Dead` |
| `ExpSign` | `Dead` | `ExpInt` | `Dead` | `Dead` | `Dead` |
| `ExpInt` | `Dead` | `ExpInt` | `Dead` | `Dead` | `Dead` |
| `Dead` | `Dead` | `Dead` | `Dead` | `Dead` | `Dead` |

检查样例：

```text
+12        Start -> Sign -> Int -> Int       accept
-0.5       Start -> Sign -> Int -> Frac      accept
.25        Start -> DotNoInt -> Frac -> Frac accept
3.         Start -> Int -> Frac              accept
2e10       Start -> Int -> ExpMark -> ExpInt accept
-6.02E+23  ... -> Frac -> ExpMark -> ExpSign -> ExpInt accept
+          Sign                              reject
.          DotNoInt                          reject
e10        Dead                              reject
1e         ExpMark                           reject
1e+        ExpSign                           reject
```

关键是：指数标记后必须至少一个数字；单独符号和单独小数点都不能接受。

</details>

## 2. LALR(1) DFA 和 parsing table

对文法 `S' -> S; S -> C C; C -> c C | d` 构造 LR(1) 项集族，合并同 LR(0) core 的项得到 LALR(1) DFA，并写出 ACTION/GOTO 表。

<details class="answer-drawer">
<summary>参考答案</summary>

增广文法：

```text
0. S' → S
1. S  → C C
2. C  → c C
3. C  → d
```

先构造 canonical LR(1)。closure 规则：

```text
[A → α . B β, a]
对 B → γ 加 [B → . γ, b]
其中 b ∈ FIRST(βa)
```

`I0`：

```text
[S' → . S, $]
[S  → . C C, $]
```

对 `[S → . C C, $]`，点后是第一个 `C`，`βa = C$`，所以：

```text
FIRST(C$) = {c,d}
```

加入：

```text
[C → . c C, c]
[C → . c C, d]
[C → . d, c]
[C → . d, d]
```

得到主要项集：

```text
I0:
  [S' → . S, $]
  [S  → . C C, $]
  [C  → . c C, c/d]
  [C  → . d, c/d]

I1 = goto(I0,S):
  [S' → S ., $]

I2 = goto(I0,C):
  [S → C . C, $]
  [C → . c C, $]
  [C → . d, $]

I3 = goto(I0,c):
  [C → c . C, c/d]
  [C → . c C, c/d]
  [C → . d, c/d]

I4 = goto(I0,d):
  [C → d ., c/d]

I5 = goto(I2,C):
  [S → C C ., $]

I6 = goto(I2,c):
  [C → c . C, $]
  [C → . c C, $]
  [C → . d, $]

I7 = goto(I2,d):
  [C → d ., $]

I8 = goto(I3,C):
  [C → c C ., c/d]

I9 = goto(I6,C):
  [C → c C ., $]
```

LALR 合并相同 LR(0) core：

```text
core(I3) = core(I6) = { C → c . C, C → . c C, C → . d }
合并为 I36，lookahead = {c,d,$}

core(I4) = core(I7) = { C → d . }
合并为 I47，lookahead = {c,d,$}

core(I8) = core(I9) = { C → c C . }
合并为 I89，lookahead = {c,d,$}
```

LALR 状态可写为：

```text
0: I0
1: I1
2: I2
3: I36
4: I47
5: I5
6: I89
```

ACTION/GOTO：

| 状态 | `c` | `d` | `$` | GOTO `S` | GOTO `C` |
|---|---|---|---|---|---|
| 0 | s3 | s4 |  | 1 | 2 |
| 1 |  |  | accept |  |  |
| 2 | s3 | s4 |  |  | 5 |
| 3 | s3 | s4 |  |  | 6 |
| 4 | r3 | r3 | r3 |  |  |
| 5 |  |  | r1 |  |  |
| 6 | r2 | r2 | r2 |  |  |

其中：

```text
r1: S → C C
r2: C → c C
r3: C → d
```

检查点：合并后没有同一格出现两个 reduce，也没有 shift/reduce 冲突，因此该文法是 LALR(1)。

</details>

## 3. static link 和 display

给定嵌套函数：`f` 内定义 `g`，`g` 内定义 `h`。`h` 访问 `f` 的局部变量 x 和 `g` 的局部变量 y。画出 static link 链，并说明 display 如何访问 x/y。

<details class="answer-drawer">
<summary>参考答案</summary>

词法嵌套关系：

```text
f
  g
    h
```

调用 `h` 时的 static link 链：

```text
h frame --static link--> g frame --static link--> f frame
```

访问步骤：

```text
h 访问 y:
  y 定义在 g 中
  从 h frame 沿 1 条 static link 到 g frame
  再按 y 在 g frame 中的 offset 取值

h 访问 x:
  x 定义在 f 中
  从 h frame 沿 2 条 static link 到 f frame
  再按 x 在 f frame 中的 offset 取值
```

伪 IR 形式：

```text
g_fp = MEM(h_fp + staticLinkOffset)
y    = MEM(g_fp + offset_y)

f_fp = MEM(g_fp + staticLinkOffset)
x    = MEM(f_fp + offset_x)
```

display 做法：

```text
display[level(f)] = 当前 f frame pointer
display[level(g)] = 当前 g frame pointer
display[level(h)] = 当前 h frame pointer
```

所以：

```text
h 访问 y: fp = display[level(g)]，再加 offset_y
h 访问 x: fp = display[level(f)]，再加 offset_x
```

区别：static link 访问外层变量需要沿链走若干步；display 可按词法层级 O(1) 取到对应 frame pointer，但调用进入/退出时要维护 display。

</details>

## 4. 给定 Tiger 语法做 IR 翻译

把 `if a < b then x := a + 1 else x := b + 1` 翻译成 Tree IR。要求使用 `CJUMP`、`LABEL`、`JUMP`、`MOVE`，并避免 fall-through 语义不清。

<details class="answer-drawer">
<summary>参考答案</summary>

源语义：

```text
if a < b then
  x := a + 1
else
  x := b + 1
```

作为 statement 翻译：

```text
CJUMP(LT, TEMP a, TEMP b, Ltrue, Lfalse)

LABEL Ltrue
MOVE(TEMP x, BINOP(PLUS, TEMP a, CONST 1))
JUMP(NAME Ldone)

LABEL Lfalse
MOVE(TEMP x, BINOP(PLUS, TEMP b, CONST 1))
JUMP(NAME Ldone)

LABEL Ldone
```

控制流检查：

```text
a < b 为真  -> Ltrue  -> x=a+1 -> Ldone
a < b 为假  -> Lfalse -> x=b+1 -> Ldone
```

`Ltrue` 后必须跳到 `Ldone`，否则会继续落入 `Lfalse`，把 `x` 又覆盖成 `b+1`。

如果这个 `if` 是有值表达式，例如：

```text
y := if a < b then a + 1 else b + 1
```

可先建临时变量：

```text
CJUMP(LT, TEMP a, TEMP b, Ltrue, Lfalse)
LABEL Ltrue
MOVE(TEMP t, BINOP(PLUS, TEMP a, CONST 1))
JUMP(NAME Ldone)
LABEL Lfalse
MOVE(TEMP t, BINOP(PLUS, TEMP b, CONST 1))
JUMP(NAME Ldone)
LABEL Ldone
```

规范化后用 `TEMP t` 表示表达式结果。

</details>

## 5. 画 basic block 并找最长 trace

对线性语句 `L0: a:=0; L1: if a<n goto L2 else L5; L2: b:=b+a; L3: a:=a+1; jump L1; L5: return b` 划分基本块，画 CFG，并选择一条最长 trace。

<details class="answer-drawer">
<summary>参考答案</summary>

原线性语句：

```text
L0: a := 0
L1: if a < n goto L2 else L5
L2: b := b + a
L3: a := a + 1
    jump L1
L5: return b
```

划分 basic block 前先补规则：

```text
1. 每个 block 以 LABEL 开头。
2. 每个 block 以 JUMP/CJUMP/RETURN 结尾。
3. 中间遇到 LABEL，要结束上一个 block，必要时补 JUMP。
```

基本块：

```text
B0:
  LABEL L0
  a := 0
  JUMP L1

B1:
  LABEL L1
  CJUMP(LT, a, n, L2, L5)

B2:
  LABEL L2
  b := b + a
  LABEL L3
  a := a + 1
  JUMP L1

B5:
  LABEL L5
  RETURN b
```

如果严格要求 block 中间不能出现第二个 LABEL，则把 `L3` 单独拿出来：

```text
B2: LABEL L2; b := b+a; JUMP L3
B3: LABEL L3; a := a+1; JUMP L1
```

CFG：

```text
B0 -> B1
B1 -> B2   // true
B1 -> B5   // false
B2 -> B1
```

一条最长/热 trace：

```text
B0 -> B1 -> B2
```

因为 `B2` 回到 `B1`，而 `B1` 已在 trace 中，停止。剩余退出块：

```text
B5
```

trace scheduling 后可让循环体路径尽量 fall-through，把退出路径放到后面。

</details>

## 6. maximal munch 指令选择

对 `MOVE(MEM(BINOP(PLUS, TEMP fp, CONST -16)), BINOP(MUL, TEMP a, CONST 4))`，在支持 `store M[base+k] <- r`、`muli r <- r,k` 的机器上用 maximal munch 生成指令。

<details class="answer-drawer">
<summary>参考答案</summary>

IR：

```text
MOVE(
  MEM(BINOP(PLUS, TEMP fp, CONST -16)),
  BINOP(MUL, TEMP a, CONST 4))
```

树形结构：

```text
          MOVE
        /      \
      MEM      MUL
       |      /   \
     PLUS   a    4
    /   \
  fp   -16
```

maximal munch 自顶向下匹配最大 tile：

```text
左侧:
  MEM(BINOP(PLUS, TEMP fp, CONST -16))
  可匹配 store addressing mode: M[fp-16]

右侧:
  BINOP(MUL, TEMP a, CONST 4)
  可匹配 immediate multiply: muli
```

生成顺序要先算右侧值，再 store：

```text
muli t <- a, 4
store M[fp-16] <- t
```

如果机器有更大模板：

```text
store M[base+k] <- r * c
```

则 maximal munch 可能一条 store-mul tile 覆盖更多节点。但题目给定模板下，上面两条是自然答案。

</details>

## 7. 活跃分析

对 `1:a<-b+c; 2:d<-a+e; 3:if d goto 1; 4:return a`，写出数据流方程并求 in/out 集。

<details class="answer-drawer">
<summary>参考答案</summary>

先列 use/def/succ：

| 行 | 语句 | use | def | succ |
|---|---|---|---|---|
| 1 | `a <- b + c` | `{b,c}` | `{a}` | `{2}` |
| 2 | `d <- a + e` | `{a,e}` | `{d}` | `{3}` |
| 3 | `if d goto 1` | `{d}` | `{}` | `{1,4}` |
| 4 | `return a` | `{a}` | `{}` | `{}` |

方程：

```text
out[n] = union(in[s] for s in succ[n])
in[n]  = use[n] union (out[n] - def[n])
```

因为有回边 `3 -> 1`，需要迭代到固定点。最终：

| 行 | in | out |
|---|---|---|
| 1 | `{b,c,e}` | `{a,b,c,e}` |
| 2 | `{a,b,c,e}` | `{a,b,c,d,e}` |
| 3 | `{a,b,c,d,e}` | `{a,b,c,e}` |
| 4 | `{a}` | `{}` |

代入检查：

```text
out[3] = in[1] ∪ in[4]
       = {b,c,e} ∪ {a}
       = {a,b,c,e}

in[3] = use[3] ∪ (out[3] - def[3])
      = {d} ∪ ({a,b,c,e} - {})
      = {a,b,c,d,e}
```

这题的关键是循环回边会让 `b/c/e` 在第 3 行 out 中重新出现。

</details>

## 8. 寄存器分配（priority 计算与选择）

给定 K=3，临时变量 spill cost：`t1=20,t2=12,t3=8,t4=4,t5=2`；相干边：`t1-t2,t1-t3,t1-t4,t2-t3,t2-t5,t3-t4,t4-t5`。计算 `cost/degree` priority，并选择潜在 spill。

<details class="answer-drawer">
<summary>参考答案</summary>

先算 degree 和 spill priority：

```text
t1: adjacent to t2,t3,t4       degree 3
t2: adjacent to t1,t3,t5       degree 3
t3: adjacent to t1,t2,t4       degree 3
t4: adjacent to t1,t3,t5       degree 3
t5: adjacent to t2,t4          degree 2
```

priority 用：

```text
priority = spill cost / degree
```

| temp | cost | degree | priority |
|---|---:|---:|---:|
| `t1` | 20 | 3 | 6.67 |
| `t2` | 12 | 3 | 4.00 |
| `t3` | 8 | 3 | 2.67 |
| `t4` | 4 | 3 | 1.33 |
| `t5` | 2 | 2 | 1.00 |

一般 priority 越低，越适合作为 spill candidate。

但 `K=3` 时，simplify 优先找：

```text
degree < K
```

`t5` 的 degree 是 2，小于 3，所以可以先 simplify，不必立刻 spill：

```text
push t5; remove t5
```

删除 `t5` 后：

```text
degree(t2) 从 3 降到 2
degree(t4) 从 3 降到 2
```

后续可以继续 simplify `t2` 或 `t4`。如果某一步剩余节点全是 degree ≥ K，再按最低 priority 选 potential spill；在高度数节点中 `t4` 的 priority 最低，更适合作 potential spill。

注意：potential spill 不等于 actual spill。select 阶段如果仍能找到颜色，就不需要真正 rewrite。

</details>
