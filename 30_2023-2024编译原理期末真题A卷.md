# 2023-2024 编译原理期末真题 A 卷
Date: 2024-06-25
Author: Yixun Hong
Tags: Compiler, Final, Past Paper, Tiger
Abstract: 根据 `24期末.pdf` 整理的 2023-2024 春夏学期编译原理期末 A 卷。题面按原卷顺序录入，每题后提供可折叠参考答案。

## 使用说明

- 原卷 PDF 可在 [materials/24期末.pdf](materials/24期末.pdf) 查看。
- 判断题和选择题按原题顺序整理；第 7、8 大题含图形，答案用文字化 IR/干涉图描述给出可核查过程。
- 编译原理大题常有多种合法写法，参考答案重点看算法步骤、控制流和约束是否正确。

## 一、判断题

### 判断题 1

**题面：** When using the subset construction method, in the worst case, an n-state NFA will be converted into a \(2^n\)-state DFA.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：True**

子集构造把 DFA 的一个状态定义为 NFA 状态集合的一个子集。`n` 个 NFA 状态最多有 `2^n` 个子集，所以最坏情况下 DFA 状态数可以达到 `2^n`。

</details>

### 判断题 2

**题面：** The regular expression in the paper can accept all C-style comments.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：False**

C 风格注释应从 `/*` 开始，到第一个 `*/` 结束。判断这类正则题时要检查两件事：

```text
1. 是否会提前漏掉第一个 */
2. 是否会错误吞掉中间已经结束的 */
```

原卷给出的表达式属于课堂常见陷阱，不能保证精确覆盖所有边界情况。

</details>

### 判断题 3

**题面：** The grammar `S -> (S)S | ε` is LL(1).

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：True**

两个候选右部的 FIRST 集：

```text
FIRST((S)S) = {"("}
FIRST(ε)    = {ε}
```

当 lookahead 是 `(` 时选第一条；当 lookahead 在 `FOLLOW(S)` 中时选 `ε`。`(` 不在需要选择 `ε` 的 FOLLOW 场景中，因此可用一个 lookahead 决定产生式。

</details>

### 判断题 4

**题面：** Imperative-style environments is usually not considered to be implemented using hash tables.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：False**

命令式符号表常用 hash table 加作用域标记或 undo stack：

```text
beginScope()
insert(name, binding)
lookup(name)
endScope()  // 撤销本层插入
```

functional-style environment 更强调持久化旧版本，常用树结构。

</details>

### 判断题 5

**题面：** A static link of a function's stack frame points to its caller's stack frame.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：False**

static link 指向词法外层函数的活动记录，不一定是运行时调用者。dynamic link 才通常指向调用者栈帧。

</details>

### 判断题 6

**题面：** Escaping variables must be kept in the registers.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：False**

escaping variable 可能被内部函数访问，或需要稳定地址，因此通常要放在 frame slot 中。寄存器没有稳定内存地址，也不能直接被内层函数通过 static link 找到。

</details>

### 判断题 7

**题面：** A display is an array for maintaining frame pointers.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：True**

display 按词法嵌套深度保存最近一次进入该深度函数的 frame pointer：

```text
display[1] = 当前 depth 1 函数的 fp
display[2] = 当前 depth 2 函数的 fp
...
```

这样访问外层变量时可直接索引对应层级。

</details>

### 判断题 8

**题面：** Lexical analysis, syntax analysis, and semantic analysis are tasks that should be completed by the frontend of a compiler.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：True**

词法分析、语法分析、语义分析属于 front end。它们把源程序转换到 AST/IR，并检查语言层面的约束。

</details>

### 判断题 9

**题面：** The task of instruction selection is to tile the IR tree with a minimal set of tree patterns.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：False**

instruction selection 的核心确实是用 machine instruction patterns/tiles 覆盖 IR tree，但目标不是简单的“pattern 数量最少”。真实选择要看 cost、寻址模式、机器指令约束和后续寄存器分配影响。

更准确的说法是：

```text
select a set of instruction tiles that covers the IR tree with good/low cost
```

</details>

### 判断题 10

**题面：** Using Maximal Munch algorithm, the optimum tiling can be obtained.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：False**

Maximal Munch 是贪心算法，从 root 开始选当前能匹配的最大 tile。它能产生合法的 tiling，但不保证全局最低 cost。

术语区分：

```text
optimal tiling  = 合法、局部可接受的覆盖
optimum tiling  = 全局最低 cost 的覆盖
```

要保证 `optimum` 通常用 dynamic programming。

</details>

### 判断题 11

**题面：** The liveness of variables should be analyzed from future to past.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：True**

liveness 是 backward dataflow analysis：

```text
out[n] = union(in[s] for s in succ[n])
in[n]  = use[n] union (out[n] - def[n])
```

当前是否 live 取决于未来是否会被使用。

</details>

### 判断题 12

**题面：** The solution to the dataflow equation is a conservative approximation.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：True**

数据流分析通常无法精确知道所有运行时路径，只能给保守近似。对 liveness 来说，宁可多认为变量 live，也不能漏掉真实会被使用的变量。

</details>

### 判断题 13

**题面：** Via tree canonicalization, one can eliminate all the CALL instructions in the IR tree.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：False**

canonicalization 会把 `CALL` 提到安全位置，例如：

```text
MOVE(TEMP t, CALL(f, args))
EXP(CALL(f, args))
```

它不是删除所有调用。函数调用仍然存在，只是不会深嵌在任意表达式里。

</details>

### 判断题 14

**题面：** A node d dominates a node n if some path of directed edges from start node s0 to n goes through d.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：False**

支配的定义是：

```text
d dominates n iff every path from start to n goes through d
```

题面把 `every path` 错写成 `some path`。

</details>

### 判断题 15

**题面：** `MOVE(MEM(x), y)` commutes with `MEM(z)` if `x != z`.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：True（按课堂简化别名假设）**

`MOVE(MEM(x), y)` 写地址 `x`，`MEM(z)` 读地址 `z`。若题目明确 `x != z`，课堂简化下认为二者访问不同内存位置，因此可交换。

真实编译器还要考虑 alias analysis；本题按课程抽象模型作答。

</details>

### 判断题 16

**题面：** The general rewriting rule rewrites `[e1, e2, ESEQ(s, e3)]` into `(SEQ(MOVE(t1, e1), s); [TEMP(t1), e2, e3])` if `s` commutes with `e1`, yet not with `e2`.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：False**

列表重排要保持原来的求值顺序：

```text
e1, e2, s, e3
```

如果 `s` 与 `e1` 可交换，但与 `e2` 不可交换，真正需要保存的是 `e2`，而不是 `e1`。题面把临时变量保存对象写错了。

</details>

### 判断题 17

**题面：** For graph coloring with coalescing, if neither simplify nor coalesce applies, we should first select a node for potential spilling.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：False**

iterated register coalescing 的典型顺序是：

```text
Simplify -> Coalesce -> Freeze -> Potential Spill
```

当不能 simplify 也不能 coalesce 时，如果还有 low-degree move-related node，应先 freeze，放弃部分 move 的合并机会，再继续 simplify/coalesce。

</details>

### 判断题 18

**题面：** In an interference graph, if there is a MOVE between `a` and `b`, and for every neighbor `t` of `b`, either `t` already interferes with `a` or `t` is of insignificant degree, then `a` and `b` can be coalesced.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：True**

这是 George criterion 的表述：

```text
for every neighbor t of b:
  t interferes with a, or
  degree(t) < K
```

合并不会给 high-degree 邻居引入新的危险约束，因此可保守合并。

</details>

### 判断题 19

**题面：** The records whose reference counts are not zero will not be reclaimed by the mark-and-sweep collection.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：False**

mark-and-sweep 按 reachability 回收对象，不看 reference count。循环垃圾的 reference count 可能不为 0，但只要从 roots 不可达，mark-sweep 仍能回收。

</details>

### 判断题 20

**题面：** Using breadth-first copying collection instead of depth-first copying can lead to the increase of cache misses.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：True**

课堂常用直觉是：DFS 沿对象引用链深入，访问局部性更强；BFS 维护前沿队列，可能把访问分散到更多对象区域，因此 cache miss 可能增加。具体效果仍取决于对象布局和程序行为。

</details>

## 二、单项选择题

### 单选题 1

**题面：** Which language can be recognized by the DFA in the graph?

A. String of binary numbers that can be divided by 2  
B. String of binary numbers that can be divided by 3  
C. String of binary numbers that can be divided by 5  
D. String of binary numbers that can be divided by 7

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：B**

题图的状态表示读入二进制串后的余数，若有 3 个状态，转移按：

```text
read 0: r -> (2r) mod 3
read 1: r -> (2r+1) mod 3
```

起始和接受状态都是余数 0，所以识别能被 3 整除的二进制数串。

</details>

### 单选题 2

**题面：** Given the grammar:

```text
exp    -> exp addop term | term
addop  -> + | -
term   -> term mulop factor | factor
mulop  -> *
factor -> (exp) | number
```

Which choice is `FIRST(factor)` and `FOLLOW(factor)`?

A. `FIRST(factor) = {(, number}`, `FOLLOW(factor) = {+, -, *, ), ε}`  
B. `FIRST(factor) = {(, number, ε}`, `FOLLOW(factor) = {$, +, -, *, )}`  
C. `FIRST(factor) = {number}`, `FOLLOW(factor) = {+, -, *, )}`  
D. `FIRST(factor) = {(, number}`, `FOLLOW(factor) = {$, +, -, *, )}`

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：D**

`factor` 的产生式只有：

```text
factor -> ( exp ) | number
```

所以：

```text
FIRST(factor) = {"(", number}
```

`factor` 出现在：

```text
term -> term mulop factor
term -> factor
```

因此可跟在 `term` 后面的符号也会进入 `FOLLOW(factor)`；同时 `term` 内部还能继续跟 `*`。最终：

```text
FOLLOW(factor) = {$, +, -, *, )}
```

</details>

### 单选题 3

**题面：** Here is the LR(0) parsing table for `S -> (S)S | ε`. Current parsing stack is:

```text
#0(2S3)4S5
```

Input is:

```text
$
```

What is the next symbol on the top of the stack?

A. `S5`  
B. `S3`  
C. `S2`  
D. `S1`

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：D**

状态 5 遇到 `$` 时按表规约：

```text
S -> (S)S
```

栈为：

```text
#0 (2 S3 )4 S5
```

右部有 4 个语法符号：`(`、`S`、`)`、`S`。规约时弹出 4 个符号及其状态：

```text
弹出: S5, )4, S3, (2
剩余: #0
```

然后查：

```text
GOTO(0, S) = 1
```

所以新栈顶为：

```text
S1
```

</details>

### 单选题 4

**题面：** When using YACC, the value of `pos` in the declaration `exp : exp PLUS pos exp` stores in:

A. `$1`  
B. `$2`  
C. `$3`  
D. `$4`

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：C**

Yacc 右部语义值从 1 开始编号：

```text
exp : exp PLUS pos exp
      $1  $2   $3  $4
```

因此 `pos` 的语义值是 `$3`。

</details>

### 单选题 5

**题面：** Which action is not in a LR parsing table?

A. Match  
B. Reduce  
C. Accept  
D. Shift

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：A**

LR parsing table 的 ACTION 通常包括：

```text
shift
reduce
accept
error / blank
```

`match` 是递归下降或 LL 分析里常见的“匹配一个终结符”的说法，不是 LR ACTION 表项。

</details>

### 单选题 6

**题面：** To access a local frame-resident variable of the current function, we should find its offset from ________.

A. The stack pointer  
B. The static link  
C. The beginning of the program  
D. The frame pointer

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：D**

frame-resident local 通常按 frame pointer 加固定 offset 访问：

```text
MEM(fp + offset_x)
```

stack pointer 可能随临时压栈变化，不适合作为稳定基准。

</details>

### 单选题 7

**题面：** If we want to access the formal parameter of an enclosing function, _____ is used to find the activation record of the enclosing function.

A. Static link  
B. Program counter  
C. Stack pointer  
D. Argument pointer

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：A**

访问词法外层函数的形参或局部变量时，要沿 static link 找到外层 activation record，再按 offset 取值。

</details>

### 单选题 8

**题面：** Which of the following is commonly found in a stack frame (activation record)?

A. Static variable  
B. Sp pointer  
C. Saved fp pointer  
D. Global variable

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：C**

普通调用帧中常见内容包括：

```text
saved fp
return address
formals / escaping locals
saved registers
temporaries / spill slots
```

全局变量和静态变量通常不在某次函数调用的活动记录中。

</details>

### 单选题 9

**题面：** For symbol table of functional style, ______ is the most efficient data structure among the following structures.

A. Octree  
B. Hash table  
C. Binary search tree  
D. Array

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：C**

functional-style symbol table 需要保留旧版本，便于退出作用域时回到之前的 environment。持久化树结构比 destructive hash table 更自然。

</details>

### 单选题 10

**题面：** Tiling the IR tree is the fundamental idea of instruction selection. ______ is not directly related with tiling.

A. Tree pattern  
B. Maximal Munch  
C. Dynamic Programming  
D. Register allocation

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：D**

tree pattern、Maximal Munch、Dynamic Programming 都直接服务于 tiling。Register allocation 是后续阶段，负责把 temporaries 分配到物理寄存器。

</details>

### 单选题 11

**题面：** Which of the method does not be used by the dynamic-programming algorithm?

A. Bottom-up  
B. Minimum-cost match  
C. Optimum solution  
D. Tiny tiles

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：D**

DP tiling 的核心是：

```text
bottom-up 计算每个节点最低 cost
选择 minimum-cost match
得到全局 optimum tiling
```

`tiny tiles` 是为了保证指令模式能覆盖所有 IR 节点的兜底 tile，不是 dynamic-programming 算法本身的方法。

</details>

### 单选题 12

**题面：** The Tree Grammars can be used in ______.

A. Fast matching  
B. Optimal tiling  
C. Code-generator generator  
D. Maximal Munch

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：C**

Tree grammar 用非终结符和产生式描述机器指令模式，可作为 code-generator generator 的输入，让工具生成匹配和选择代码。

</details>

### 单选题 13

**题面：** The ______ is not related to the liveness information about a node N.

A. `Def[N]`  
B. `Use[N]`  
C. `Pred[N]`  
D. `Succ[N]`

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：C**

liveness 方程直接使用：

```text
in[N]  = use[N] union (out[N] - def[N])
out[N] = union(in[S] for S in succ[N])
```

`pred[N]` 可用于遍历或求解顺序，但不是这个方程直接需要的信息。

</details>

### 单选题 14

**题面：** In the following descriptions about interference graphs, _____ is incorrect.

A. It can be expressed as a matrix.  
B. It is only used for register allocation.  
C. It is caused by overlapping live ranges.  
D. It is conservative approximation.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：B**

interference graph 的考试核心用途是 register allocation，但“only used”过于绝对。A、C、D 都符合课程口径：

```text
可用邻接矩阵/邻接表表示
边来自 live range overlap
基于 liveness 的保守近似
```

</details>

### 单选题 15

**题面：** Which of following choice is a valid canonical tree?

A. `ESEQ(MOVE(MEM(TEMP fp), 10), MEM(TEMP fp))`  
B. `MOVE(MEM(CALL(f, args), TEMP a))`  
C. `SEQ(MOVE(TEMP r, 1), CJUMP(<, TEMP r, 0, t, f))`  
D. `MOVE(TEMP a, CALL(g, args))`

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：D**

canonical tree 中：

```text
不能有 ESEQ
CALL 不能深嵌在任意表达式里
CALL 允许出现在 EXP(CALL(...)) 或 MOVE(TEMP t, CALL(...))
```

所以 `MOVE(TEMP a, CALL(g, args))` 是合法的 canonical 形式。

</details>

### 单选题 16

**题面：** How many basic blocks should the following IR program be divided into?

```text
MOV(TEMP x, MEM(TEMP fp))
CJUMP(<, TEMP x, 5, t, f)
LABEL t
MOVE(TEMP r, 1)
CJUMP(>, a, b, t1, f1)
LABEL(f1)
MOVE(TEMP r, 0)
LABEL(t1)
JUMP join
LABEL f
MOVE(TEMP r, 0)
JUMP join
LABEL join
RET r
```

A. 5  
B. 6  
C. 7  
D. 8

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：B**

按规则划分：

```text
B1: MOV; CJUMP(<,...,t,f)
B2: LABEL t; MOVE; CJUMP(>,...,t1,f1)
B3: LABEL f1; MOVE; JUMP t1      // 遇到下一个 LABEL 前补跳转
B4: LABEL t1; JUMP join
B5: LABEL f; MOVE; JUMP join
B6: LABEL join; RET r
```

因此共 6 个 basic blocks。

</details>

### 单选题 17

**题面：** Among the following transformations of IR trees, which one is wrong?

A. `ESEQ(s1, ESEQ(s2, e)) => ESEQ(SEQ(s1, s2), e)`  
B. `JUMP(ESEQ(s, e1)) => ESEQ(s, JUMP(e1))`  
C. `BINOP(op, ESEQ(s, e1), e2) => ESEQ(s, BINOP(op, e1, e2))`  
D. `CJUMP(op, CONST 1897, ESEQ(s, e), l1, l2) => SEQ(s, CJUMP(op, CONST 1897, e, l1, l2))`

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：B**

`JUMP` 是 statement，`ESEQ` 是 expression 形式，不能把 statement 包进 `ESEQ` 当表达式返回。正确思路应是把 `s` 外提成 statement sequence：

```text
JUMP(ESEQ(s, e1))  =>  SEQ(s, JUMP(e1))
```

C 在严格语义下还需要 `s` 与 `e2` 可交换；本题最明显、类型上直接错误的是 B。

</details>

### 单选题 18

**题面：** A definition `d <- a1 ⊕ a2` may not be loop-invariant within a loop L if:

A. `ai` are constants.  
B. All the definitions `ai` that reaches `d` are outside L.  
C. Only one definition of `ai` reaches `d`, and that definition is loop-invariant.  
D. `d` is not used in L.

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：D**

循环不变计算主要看右侧操作数的 reaching definitions：

```text
1. 操作数是常量；或
2. 所有 reaching definitions 都在循环外；或
3. 唯一 reaching definition 本身已经是 loop-invariant
```

`d` 是否在循环内使用不是判断定义是否 loop-invariant 的条件；它只影响是否值得外提。

</details>

### 单选题 19

**题面：** ___ does not suffer from external fragmentation?

A. Mark-and-Sweep Collection  
B. Reference Counts  
C. Copying Collection  
D. Neither of the above

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：C**

copying collection 把可达对象复制到 to-space，形成紧凑连续区域，因此不会产生传统意义上的 external fragmentation。非移动式 mark-sweep 和 reference counting 都可能留下碎片。

</details>

### 单选题 20

**题面：** Suppose the target machine has 3 registers. To color the interference graph in the paper, under what criterion can `c` and `d` be coalesced?

A. Briggs criterion  
B. George criterion  
C. Both of the above  
D. Neither of the above

<details class="answer-drawer">
<summary>参考答案</summary>

**答案：B**

题图中 `c-d` 是 move edge。按 `K=3`：

```text
Briggs:
  合并 cd 后，高度数邻居数量达到 K，不能证明安全。

George:
  以 d 的邻居检查，d 的主要邻居 b/e 都已经与 c 干涉，
  因此合并 d 到 c 不会给这些高阶邻居新增危险约束。
```

所以可用 George criterion，但不能用 Briggs criterion。

</details>

## 三、大题

### 大题 1：带小数和指数的 number DFA

**题面：** Draw the DFA for the number.

```text
nat       = [0-9]+
signedNat = ("+" | "-")? nat
number    = signedNat ("." nat)? (E signedNat)?
```

<details class="answer-drawer">
<summary>参考答案</summary>

把结构拆成三段：

```text
可选符号 + 至少一位数字
可选小数部分: "." 后必须至少一位数字
可选指数部分: "E" 后是 signedNat
```

DFA 状态：

| 状态 | 含义 | 接受 |
|---|---|---|
| `S` | 起点 | 否 |
| `SIGN` | 读到开头 `+/-` | 否 |
| `INT` | 整数部分已有至少一位数字 | 是 |
| `DOT` | 读到小数点，但小数位还没有数字 | 否 |
| `FRAC` | 小数部分已有至少一位数字 | 是 |
| `EXP` | 读到 `E` | 否 |
| `EXP_SIGN` | 指数部分读到 `+/-` | 否 |
| `EXP_INT` | 指数部分已有至少一位数字 | 是 |
| `DEAD` | 非法 | 否 |

转移表：

| 当前 | `+/-` | digit | `.` | `E` | other |
|---|---|---|---|---|---|
| `S` | `SIGN` | `INT` | `DEAD` | `DEAD` | `DEAD` |
| `SIGN` | `DEAD` | `INT` | `DEAD` | `DEAD` | `DEAD` |
| `INT` | `DEAD` | `INT` | `DOT` | `EXP` | `DEAD` |
| `DOT` | `DEAD` | `FRAC` | `DEAD` | `DEAD` | `DEAD` |
| `FRAC` | `DEAD` | `FRAC` | `DEAD` | `EXP` | `DEAD` |
| `EXP` | `EXP_SIGN` | `EXP_INT` | `DEAD` | `DEAD` | `DEAD` |
| `EXP_SIGN` | `DEAD` | `EXP_INT` | `DEAD` | `DEAD` | `DEAD` |
| `EXP_INT` | `DEAD` | `EXP_INT` | `DEAD` | `DEAD` | `DEAD` |
| `DEAD` | `DEAD` | `DEAD` | `DEAD` | `DEAD` | `DEAD` |

样例检查：

```text
12        S -> INT                         accept
-12       S -> SIGN -> INT                 accept
+3.14     S -> SIGN -> INT -> DOT -> FRAC  accept
2E10      S -> INT -> EXP -> EXP_INT       accept
-6.02E+23 ... -> FRAC -> EXP -> EXP_SIGN -> EXP_INT accept

+         SIGN       reject
.5        DEAD       reject
3.        DOT        reject
1E        EXP        reject
1E+       EXP_SIGN   reject
```

注意：本题的 `number` 不接受 `.5` 或 `3.`，因为小数点前后都由给定正则约束。

</details>

### 大题 2：LALR(1) DFA 和 parsing table

**题面：** Given the grammar:

```text
0 A' -> A $
1 A  -> ( A )
2 A  -> a
```

1. Construct the LALR(1) DFA for the grammar.  
2. Construct the LALR(1) parsing table.

<details class="answer-drawer">
<summary>参考答案</summary>

把 `$` 当 EOF marker。为了填表简洁，下面使用常见约定：状态含 `[A' -> A . $, $]` 时，在 `$` 上写 `accept`。

LR(1) 初始项集：

```text
I0:
  [A' -> . A $, $]
  [A  -> . ( A ), $]
  [A  -> . a, $]
```

关键 closure：

```text
[A' -> . A $, $]
点后是 A，beta 是 $
FIRST($ $) = {$}
所以 A 的产生式 lookahead 为 $
```

从 `I0` 出发：

```text
goto(I0, A) = I1:
  [A' -> A . $, $]

goto(I0, "(") = I2:
  [A -> ( . A ), $]
  [A -> . ( A ), )]
  [A -> . a, )]

goto(I0, "a") = I3:
  [A -> a ., $]
```

继续展开：

```text
goto(I2, A) = I4:
  [A -> ( A . ), $]

goto(I2, "(") 得到与 I2 同 LR(0) core 的状态，LALR 合并回 I2。

goto(I2, "a") 得到与 I3 同 LR(0) core 的状态：
  [A -> a ., )]
LALR 合并后 I3 lookahead = {$, )}

goto(I4, ")") = I5:
  [A -> ( A ) ., $]
递归括号内部产生的同 core 状态合并后，I5 lookahead = {$, )}
```

LALR 状态：

```text
0:
  [A' -> . A $, $]
  [A  -> . ( A ), $]
  [A  -> . a, $]

1:
  [A' -> A . $, $]

2:
  [A -> ( . A ), $/)]
  [A -> . ( A ), )]
  [A -> . a, )]

3:
  [A -> a ., $/)]

4:
  [A -> ( A . ), $/)]

5:
  [A -> ( A ) ., $/)]
```

ACTION/GOTO 表：

| 状态 | `(` | `a` | `)` | `$` | GOTO `A` |
|---|---|---|---|---|---|
| 0 | s2 | s3 |  |  | 1 |
| 1 |  |  |  | acc |  |
| 2 | s2 | s3 |  |  | 4 |
| 3 |  |  | r2 | r2 |  |
| 4 |  |  | s5 |  |  |
| 5 |  |  | r1 | r1 |  |

其中：

```text
r1: A -> ( A )
r2: A -> a
```

若采用“显式 shift EOF”的教材写法，也可把状态 1 在 `$` 上写 `s6`，再令状态 6 为 `[A' -> A $ ., $]` 并在 `$` 上 `accept`。两种表述等价。

</details>

### 大题 3：static link 和 display

**题面：** Given a Tiger program:

```text
let
  function f(): int = let
    var a := 12
    function g(y: int): int = let
      var b := 8
      function gcd(): int =
        if a = 0 then b + y
        else if b = 0 then a + y
        else if a > b then gcd()
        else gcd()
      in
        gcd()
      end
    in
      g(10)
    end
in
  f()
end
```

Function `f()` is at depth 1. Element `Di` of the display always points to the most recently called function whose static nesting depth is `i`.

1. Show the static links of activation records when `gcd()` is called a second time.  
2. When using display instead of static links, show the pointers in the display when `gcd()` is called a second time.  
3. Compare the instructions to access `a` in `gcd()` when using static links and display, where `a` is a local variable of function `f()`.

<details class="answer-drawer">
<summary>参考答案</summary>

词法嵌套深度：

```text
f    depth 1
g    depth 2
gcd  depth 3
```

第二次调用 `gcd()` 时，调用栈动态上类似：

```text
f frame
g frame
gcd frame #1
gcd frame #2
```

但 static link 不按调用者走，而按词法父函数走：

```text
gcd frame #2 --static link--> g frame
gcd frame #1 --static link--> g frame
g frame      --static link--> f frame
f frame      --static link--> outer/root
```

也就是说，递归调用 `gcd()` 时，新 `gcd` 的 static link 仍然指向 `g` frame，而不是指向上一层 `gcd` frame。

display 在第二次 `gcd()` 中：

```text
D1 -> f frame
D2 -> g frame
D3 -> gcd frame #2
```

访问 `a`：

```text
a 定义在 f，当前在 gcd，深度差 = 3 - 1 = 2
```

static link 方式：

```text
g_fp = MEM(gcd_fp + staticLinkOffset)
f_fp = MEM(g_fp + staticLinkOffset)
a    = MEM(f_fp + offset_a)
```

display 方式：

```text
f_fp = D1
a    = MEM(f_fp + offset_a)
```

比较：

| 方法 | 访问 `a` 的关键步骤 |
|---|---|
| static link | 沿 static link 走 2 次，再按 offset 取 `a` |
| display | 直接取 `D1`，再按 offset 取 `a` |

display 通常减少外层变量访问指令数，但调用进入/退出函数时要维护 display。

</details>

### 大题 4：翻译 if-and-else 到 IR

**题面：** Given the IR types in the paper, translate:

```text
if (a < b and c > d):
  x := y + z
else:
  x := y - z
```

<details class="answer-drawer">
<summary>参考答案</summary>

`and` 要短路求值。先检查 `a < b`，只有为真时才检查 `c > d`：

```text
CJUMP(LT, Var('a'), Var('b'), 'AND_RIGHT', 'ELSE')

LABEL('AND_RIGHT')
CJUMP(GT, Var('c'), Var('d'), 'THEN', 'ELSE')

LABEL('THEN')
MOVE(Var('x'), BINOP(PLUS, Var('y'), Var('z')))
JUMP('IF_END')

LABEL('ELSE')
MOVE(Var('x'), BINOP(MINUS, Var('y'), Var('z')))

LABEL('IF_END')
```

检查点：

```text
a < b 为 false: 直接跳 ELSE
a < b 为 true: 再判断 c > d
c > d 为 true: THEN
c > d 为 false: ELSE
THEN 后必须跳过 ELSE
```

标签名不唯一，只要控制流等价即可。

</details>

### 大题 5：basic blocks 和 longest trace

**题面：** Consider the following program:

```text
L1:
  t <- n % 2
  if t == 0 goto L2
  res <- res * base
L2:
  base <- base * base
  n <- n / 2
  if n > 0 goto L1
  return res
```

1. Break the program into basic blocks.  
2. Determine the longest trace.

<details class="answer-drawer">
<summary>参考答案</summary>

basic block 规则：

```text
以 LABEL 开始
以 JUMP/CJUMP/RETURN 结束
中间不能有 LABEL
若普通语句后遇到 LABEL，需要补 JUMP
```

划分：

```text
B1:
  LABEL L1
  t <- n % 2
  CJUMP(EQ, t, 0, L2, Lres)

B2:
  LABEL Lres
  res <- res * base
  JUMP L2

B3:
  LABEL L2
  base <- base * base
  n <- n / 2
  CJUMP(GT, n, 0, L1, Ldone)

B4:
  LABEL Ldone
  return res
```

CFG：

```text
B1 -> B3   // t == 0
B1 -> B2   // t != 0
B2 -> B3
B3 -> B1   // n > 0
B3 -> B4   // n <= 0
```

一种 longest trace：

```text
B1 -> B2 -> B3 -> B4
```

这条 trace 沿 `t != 0` 分支执行乘法，再进入 `L2`，最后走退出分支。trace 选择不唯一，但不能在同一轮 trace covering 中重复已经放入 trace 的 block。

</details>

### 大题 6：liveness 与 interference graph

**题面：** Perform flow analysis on the following program.

```text
1  s <- 0
2  i <- 0
3  L1: if i > n goto L2
4  j <- i * 4
5  k <- j + a
6  x <- M[k]
7  s <- s + x
8  i <- i + 1
9  goto L1
10 L2:
```

1. Calculate live-in and live-out at each statement.  
2. Construct the register interference graph.

<details class="answer-drawer">
<summary>参考答案</summary>

这里按常见求和程序理解：`L2` 后会使用最终的 `s`，因此可把第 10 行看作 exit/return point，`in[10] = {s}`。若题目明确 `L2` 后什么都不用，则只需把第 10 行的 `s` 去掉，其他循环内主要结论不变。

use/def/succ：

| 行 | use | def | succ |
|---:|---|---|---|
| 1 | `{}` | `{s}` | `{2}` |
| 2 | `{}` | `{i}` | `{3}` |
| 3 | `{i,n}` | `{}` | `{4,10}` |
| 4 | `{i}` | `{j}` | `{5}` |
| 5 | `{j,a}` | `{k}` | `{6}` |
| 6 | `{k}` | `{x}` | `{7}` |
| 7 | `{s,x}` | `{s}` | `{8}` |
| 8 | `{i}` | `{i}` | `{9}` |
| 9 | `{}` | `{}` | `{3}` |
| 10 | `{s}` | `{}` | `{}` |

方程：

```text
out[n] = union(in[s] for s in succ[n])
in[n]  = use[n] union (out[n] - def[n])
```

固定点结果：

| 行 | live-in | live-out |
|---:|---|---|
| 1 | `{a,n}` | `{a,n,s}` |
| 2 | `{a,n,s}` | `{a,i,n,s}` |
| 3 | `{a,i,n,s}` | `{a,i,n,s}` |
| 4 | `{a,i,n,s}` | `{a,i,j,n,s}` |
| 5 | `{a,i,j,n,s}` | `{a,i,k,n,s}` |
| 6 | `{a,i,k,n,s}` | `{a,i,n,s,x}` |
| 7 | `{a,i,n,s,x}` | `{a,i,n,s}` |
| 8 | `{a,i,n,s}` | `{a,i,n,s}` |
| 9 | `{a,i,n,s}` | `{a,i,n,s}` |
| 10 | `{s}` | `{}` |

干涉图按规则构造：

```text
每条定义 d 的语句，让 d 与 live-out 中除 d 外的变量连边。
```

主要干涉边：

```text
s-a, s-n, s-i, s-j, s-k, s-x
i-a, i-n, i-j, i-k, i-x, i-s
j-a, j-i, j-n, j-s
k-a, k-i, k-n, k-s
x-a, x-i, x-n, x-s
```

也可按邻接表写：

| 节点 | 邻居 |
|---|---|
| `s` | `{a,n,i,j,k,x}` |
| `i` | `{a,n,s,j,k,x}` |
| `j` | `{a,i,n,s}` |
| `k` | `{a,i,n,s}` |
| `x` | `{a,i,n,s}` |
| `a` | `{s,i,j,k,x}` |
| `n` | `{s,i,j,k,x}` |

如果只给 ordinary temporaries 画图，可把参数/base `a,n` 仍作为 live ranges 画入图；若题目只要求可分配临时变量，则重点画 `s,i,j,k,x` 及它们与 `a,n` 的约束。

</details>

### 大题 7：IR tree tiling 与 Jouette 指令

**题面：** Given the IR tree in the paper:

```text
MOVE(
  MEM(BINOP(PLUS, CONST 5, MEM(CONST 1000))),
  MEM(BINOP(PLUS, MEM(TEMP x), TEMP FP)))
```

1. Circle the tiles and number them in the order that they are munched.  
2. Generate Jouette-machine instructions using Maximal Munch.

<details class="answer-drawer">
<summary>参考答案</summary>

Maximal Munch 从 root 开始选最大 tile。根节点是：

```text
MOVE(MEM(dstAddr), srcExp)
```

因此 root 可匹配 store tile：

```text
STORE M[dstAddr] <- r_src
```

左侧地址：

```text
BINOP(PLUS, CONST 5, MEM(CONST 1000))
```

可先把 `MEM(CONST 1000)` 读入寄存器，再用 `+5` 作为 store addressing offset：

```text
LOAD r1 <- M[1000]
```

右侧值：

```text
MEM(BINOP(PLUS, MEM(TEMP x), TEMP FP))
```

先算地址：

```text
LOAD r2 <- M[x]
ADD  r2 <- r2 + FP
LOAD r3 <- M[r2]
```

最后 store：

```text
STORE M[r1 + 5] <- r3
```

一种可写的 munch 顺序：

```text
1. root STORE tile: MOVE(MEM(...), ...)
2. LOAD tile: MEM(CONST 1000)
3. LOAD tile: MEM(TEMP x)
4. ADD tile: BINOP(PLUS, r2, FP)
5. LOAD tile: MEM(r2)
6. final STORE emission
```

对应 Jouette 风格指令：

```text
LOAD  r1 <- M[1000]
LOAD  r2 <- M[x]
ADD   r2 <- r2 + FP
LOAD  r3 <- M[r2]
STORE M[r1 + 5] <- r3
```

如果所用 Jouette 表不支持 `STORE M[r+c] <- r`，则把最后两步写成：

```text
ADDI  r1 <- r1 + 5
STORE M[r1] <- r3
```

两种写法的语义相同，差异只在目标机寻址模式。

</details>

### 大题 8：spill 与寄存器分配

**题面：** A compiler is compiling a program for a target machine with three registers `r1`, `r2`, `r3`. Instruction list and interference graph are shown in the paper.

```text
enter: t <- r3
       p <- r1
       i <- r2
       m <- M[p]
       a <- M[p+4]
loop:  r1 <- a
       r2 <- i
       call f (use r1, r2, def r1)
       a <- r1
       if a > m goto end
       a <- m - a
       i <- i - 1
       if i > 0 goto loop
end:   r1 <- i
       r3 <- t
       return (r1, r3 live out)
```

1. Calculate spill priority and choose a variable to spill.  
2. Modify the program to spill the selected variable and draw the new graph.  
3. Finish register allocation using George criterion for coalescing.  
4. Rewrite the program.

<details class="answer-drawer">
<summary>参考答案</summary>

只对 ordinary temporaries 算 spill priority：

```text
t, p, i, m, a
```

`r1/r2/r3` 是 precolored physical registers，不可 spill。

按题设循环平均执行 `n` 次，priority 公式：

```text
priority = (outside_count + n * inside_count) / degree
```

根据题图/程序重算，普通节点可取以下 degree：

| temp | outside use+def | inside use+def | degree | priority |
|---|---:|---:|---:|---|
| `t` | 2 | 0 | 6 | `2/6 = 1/3` |
| `p` | 3 | 0 | 4 | `3/4` |
| `i` | 2 | 4 | 5 | `(2+4n)/5` |
| `m` | 1 | 2 | 6 | `(1+2n)/6` |
| `a` | 1 | 5 | 3 | `(1+5n)/3` |

当 `n >= 1` 时，最低 priority 是：

```text
t: 1/3
```

因此选择 spill `t`。直觉上 `t` 只是在入口保存 `r3`、出口恢复 `r3`，不在循环内频繁使用，spill 成本最低。

spill rewrite：

```text
enter:
  M[slot_t] <- r3       // 代替 t <- r3
  p <- r1
  i <- r2
  m <- M[p]
  a <- M[p+4]
loop:
  r1 <- a
  r2 <- i
  call f (use r1, r2, def r1)
  a <- r1
  if a > m goto end
  a <- m - a
  i <- i - 1
  if i > 0 goto loop
end:
  r1 <- i
  t_restore <- M[slot_t]
  r3 <- t_restore
  return (r1, r3 live out)
```

spill 后重新建图。核心变化：

```text
删除长 live range t
新增 t_restore，但它只在函数出口附近短暂 live
```

一组可行 coalescing（只用 George criterion）：

```text
p <- r1   可合并 p 与 r1
i <- r2   可合并 i 与 r2
r1 <- a / a <- r1   可合并 a 与 r1
```

不能合并：

```text
end: r1 <- i
```

因为 `i` 已合并到 `r2`，而 `r1` 与 `r2` 是不同 precolored registers，二者互相干涉。

一种合法 coloring：

| temporary | register |
|---|---|
| `p` | `r1` |
| `a` | `r1` |
| `i` | `r2` |
| `m` | `r3` |
| `t` | spilled to `slot_t` |

检查：

```text
a 与 i/m 干涉，所以 a=r1, i=r2, m=r3 合法。
p 与 i/m 干涉，但 p 在读完 M[p+4] 后死亡，可与 a 同用 r1。
m 跨 loop 保持在 r3，且原 r3 的旧值已 spill 到 slot_t。
```

最终重写程序可写为：

```text
enter:
  M[slot_t] <- r3
  // p <- r1 coalesced
  // i <- r2 coalesced
  r3 <- M[r1]        // m <- M[p], m in r3
  r1 <- M[r1+4]      // a <- M[p+4], a in r1

loop:
  // r1 <- a deleted
  // r2 <- i deleted
  call f (use r1, r2, def r1)
  // a <- r1 deleted
  if r1 > r3 goto end
  r1 <- r3 - r1
  r2 <- r2 - 1
  if r2 > 0 goto loop

end:
  r1 <- r2
  r3 <- M[slot_t]
  return (r1, r3 live out)
```

说明：寄存器分配题允许多种 simplify/coalesce 顺序和等价 coloring。只要 spill priority、rewrite、George 判据和最终每条干涉边两端不同色，就可认为正确。

</details>
